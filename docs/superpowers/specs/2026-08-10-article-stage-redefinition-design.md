# 기사 단위 이적 단계 재정의 spec — 완료 · 무산 신설 · 이적 합의 좁힘 · 협상 중 넓힘

- 날짜: 2026-08-10
- 트랙: 기사 단위 이적 단계 전면 재검토 (backlog a · 사용자 발제 2026-08-08)
- 흡수 안건: "오피셜 = 이적 완료로 읽히는 문제" (2026-08-04 · backlog g) · BBC 가십 단계 롤업 잔여 확인 (backlog h)
- 이번 회차 산출물: 이 스펙 + 버킷 순도 감사 판정 파일 (assets).
소급 재분류 · 코드 반영 · serve 구현은 다음 회차다 (§11).

## 1. 배경 · 문제

메인 목록의 영입 단계 필터 분포 (오피셜 7 · 이적 합의 124 · 협상 중 102 · 개인 합의 9 · 관심 168 · 루머 131 · 기타 63) 가 직관에 반한다는 발제 (2026-08-08) 에서 출발했다.
이적 합의가 협상 중보다 많고, 개인 합의가 9건뿐인 구성을 독자가 이해할 수 없다.

감사 실측 (§2) 으로 확인한 원인은 세 겹이며, 순수 오분류는 그중 가장 작은 축이다.

- **완료된 딜의 거처가 없다** — official 은 공홈 규칙 전용이라 (2026-07-19 확정), 타 매체의 "계약 체결 완료 · 등번호 배정 · 회고" 기사가 전부 agreed 에 영구 누적된다.
agreed 121건 중 완결 사가 후속이 80건 이상이다.
- **현행 agreed 정의가 임박 보도까지 포함한다** — "딜 확정 · 임박 보도" 를 한 버킷으로 정의했으므로 (07-19 §2), 사가의 협상 후반 · 임박 기사가 정의대로 agreed 에 뭉친다.
오분류가 아니라 정의가 시킨 결과다.
- **타 구단 간 딜이 단계 필터에 섞인다** — "이적 주체가 아스날이 아니어도 단계는 그대로 매긴다 (방향만 none)" 규칙 때문에, 완결 사가를 걷어낸 잔여 agreed 41건 중 약 24건이 타 구단 딜 · 아스날이 놓친 딜이다.

여기에 화면 문제 두 가지가 같은 뿌리로 얽힌다.

- 오피셜 배지가 "이적 완료" 로 읽히지만 실제 의미는 "공홈 공지" 다.
공홈은 합의 때 · 확정 때 각각 올리므로 같은 딜에 오피셜이 두 번 나온다 (트로사르 실측: 5267e88b 합의 → 7782fd9e 확정).
- 무산 · 잔류 확정 기사의 거처가 없어 프롬프트의 두 규칙 ("무산 → 도달했던 마지막 단계" vs "재계약 → other") 이 충돌하고, 비니시우스 잔류 같은 사가 결말이 기본 숨김 버킷 (other) 으로 사라진다.

## 2. 버킷 순도 감사 실측 (2026-08-10 · bulletin_after_0808 사본)

표본 132행 (interest · rumour · negotiating · other · agreed 각 20 무작위 + medical 10 · personal_terms 9 · official 6 전건) 을 전건 판독했다.
판정 파일: `assets/2026-08-10-article-stage-audit-judgments.tsv` — 다음 회차 dry-run 의 사전 등록 기대값으로 재사용한다.

| 버킷 | 표본 | 오분류 | 오분류율 | 비고 |
| --- | --- | --- | --- | --- |
| agreed | 20 | 1 | 5% | 현행 정의 기준으로는 깨끗 — 정의가 뭉침의 원인 |
| interest | 20 | 1 | 5% | |
| rumour | 20 | 1 | 5% | 가십 규칙 행 11 포함 |
| negotiating | 20 | 2 | 10% | |
| other | 20 | 0 | 0% | 단, 규칙 충돌 계열 4건이 숨김 버킷에 |
| medical | 10 | 0 | 0% | |
| personal_terms | 9 | 4 | 약 44% | "제시 준비 · 계획 · 근접" 을 합의로 분류하는 일관된 오염 |
| official | 6 | 0 | 0% | 공홈 소스 불변량 유지 |

추가 실측 결론.

- **방향 축 오류 실재** — out 전건 (50건) 검토에서 오류 5건 (10%) 확정: 6aa79a9d · 9356f857 · f057ba44 · 4a8397d1 · 745f1094.
전부 아스날 비주체 딜이 in/out 으로 당겨진 패턴 (none 과소) 이며, in 축에서도 1건 (aeb2d493) 확인.
- **규칙 충돌 규모 40건** — 제목에 재계약 · 잔류가 든 분류 행이 6개 버킷 (interest 18 · other 9 · rumour 6 · negotiating 5 · agreed 1 · personal_terms 1) 에 산재한다.
- **가십 규칙 무결** — bbc_gossip 이면서 rumour 가 아닌 행 0건.
backlog h 는 재검토 불요로 닫는다 (2026-08-02 A안 유지).
- **표기 갈림 함정** — 동일인 뇌르고르 / 노르가르 / 노르고르 표기가 갈려 한글 이름 LIKE 매칭이 새는 것을 확인했다.
감사 · 검증에서 이름 문자열 매칭을 신뢰하지 말고 본문으로 판정한다.
- **분류 입력의 오염 사례** — 분류 입력인 요약이 세계 지식을 주입한 사례 (a4f5fc4c 요약이 본문에 없는 "브렌트포드의" 를 생성) 와 제목 오역 사례 (ce42d34d "영입 가능성" — 본문은 내줄 의사) 를 확인했다.
전자는 입력 확장 (§4-7) 의 논거이고, 후자는 번역 신뢰성 필드 소관으로 기록만 한다.

## 3. 확정 결정 (사용자, 2026-08-09 ~ 08-10)

07-19 확정 ("이적 합의 = 합의부터 임박까지 넓은 버킷") 을 재개정한다.

- '이적 합의' 는 **진짜 확정 보도만** 담는다 — 이적 확정 + here we go 처럼 사실상 확정으로 쓰이는 보도.
- 임박 · 근접 · 눈앞 보도는 **협상 중으로 넓혀** 담는다.
- 완료된 이적의 후속 보도를 담는 **'이적 완료' (done) 를 신설**한다 (3층 구분 — 안 ㄱ).
- 아스날 관련 딜의 종결 보도를 담는 **'무산' (collapsed) 을 신설**한다 (선택지 ㉰).
- 메디컬의 사이드바 그룹 소속을 협상 중 → 이적 합의로 옮긴다.
- 단계 필터의 계수 · 목록을 방향 in · out (아스날 주체) 한정으로 바꾼다 (§8).

### 3.1 단계 체계

| 순서 | enum | 라벨 | 정의 | 경로 |
| --- | --- | --- | --- | --- |
| 1 | official | 오피셜 | 공홈 공지 (불변) | 규칙 |
| 2 | done (신설) | 이적 완료 | 완료된 이적의 후속 · 부대 보도 — 체결 완료 · 등번호 · 입단 소감 · 회고 · 해설 | LLM + 명단 주입 |
| 3 | agreed | 이적 합의 | 확정 보도만 — 구단 간 완전 합의 도달 · here we go 류 · 체결을 새 소식으로 전하는 기사 | LLM |
| 4 | medical | 메디컬 | 유지 | LLM |
| 5 | personal_terms | 개인 합의 | 유지 + "준비 · 계획 · 근접은 합의가 아니다" 명시 | LLM |
| 6 | negotiating | 협상 중 | 넓힘 — 협상 · 제안 · 거절 + 합의 임박 · 근접 · 눈앞 (확정 아님) | LLM |
| 7 | interest | 관심 | 유지 | LLM |
| 8 | rumour | 루머 | 유지 | LLM + 가십 규칙 |
| 9 | collapsed (신설) | 무산 | 아스날 관련 딜의 종결 — 잔류 확정 · 재계약 체결 · 결렬 확정 · 하이재킹 완패 | LLM |
| — | other | 기타 | 유지 (기본 숨김 토글 불변) | LLM |

- agreed ↔ negotiating 경계는 수사가 아니라 **주장 유무**로 가른다 — 기사가 합의 도달을 사실로 보도하면 agreed, 접근 중이라고 보도하면 negotiating.
- 사이드바 표시 그룹: 오피셜 / 이적 완료 / 이적 합의 (+ 메디컬 편입) / 개인 합의 / 협상 중 / 관심 / 루머 / 무산 (+ 기타 토글).
- 예시 기대 매핑 (기마랑이스 사가 3층 분해): "이적료 협상 진전 · £80m 제안 · 영입 임박" → 협상 중 · "9천만 유로 합의 · 원칙적 합의" → 이적 합의 · "계약 체결 완료 · 등번호 39번 · 발표 지연 · 회고" → 이적 완료.
- 오피셜 라벨 문구는 유지한다 — 완료 라벨이 따로 생기면서 "오피셜 = 공홈 공지 · 완료 = 딜 종결" 의 의미가 화면에서 갈라져 오독 (backlog g) 이 구조적으로 해소된다.

### 3.2 무산의 경계

경계 완화에는 제외가 따라야 한다.

- 무산은 **아스날 관련 딜의 종결**에만 쓴다.
타 구단 간 딜의 무산 · 결렬은 기존 규칙대로 그 딜이 도달했던 마지막 단계로 남긴다.
- 진행 중인 잔류 · 재계약 협상 보도는 무산이 아니다 — 결말이 확정된 보도만 무산이다.
- **하이재킹 완결 보도 (아스날이 경쟁에서 밀린 딜의 성사) 는 무산이다** — 방향은 아스날이 시도했던 축 (in/out).
당초 "주된 관점으로 가른다" 안을 등록했으나 dry-run 에서 주관점 판별이 판마다 흔들렸고 (§6.1), 모델의 자연 판독 · 아스날 팬 관점 모두 무산 쪽과 일치해 규칙을 이쪽으로 단순화했다 (사용자 확정 2026-08-10).
- 기존 프롬프트의 "무산 → 도달했던 마지막 단계" · "재계약 → other" 두 규칙 중 아스날 관련 딜 부분을 collapsed 로 대체해 충돌 (§2 의 40건 산재) 을 해소한다.

## 4. 프롬프트 · 재료 설계 (STAGE_PROMPT 개정 요지)

이번 회차는 스펙 문안까지이고 코드 반영은 다음 회차다.

1. **agreed 좁힘** — 임박 계열 문구를 제거하고 "구단 간 완전 합의 도달 · 딜 확정을 사실로 보도 (here we go · 구두 합의 · 원칙적 합의 포함)" 로 바꾼다.
2. **negotiating 넓힘** — "이적료 · 조건 협상 · 제안 · 거절 + 합의 임박 · 근접 · 눈앞 · 마무리 단계 (합의 도달을 사실로 전하지 않는 한)".
3. **done 신설** — "이미 완료된 이적의 후속 · 부대 보도 (체결 완료 · 등번호 · 입단 소감 · 회고 · 해설)".
완료 여부 판단이 세계 지식에 기대지 않도록 확정 명단 (`players` 의 in_done · out_done) 을 `{roster}` 재료로 주입한다 — 시소 트러블슈팅 (2026-08-08) 의 해법과 같은 패턴.
4. **collapsed 신설** — §3.2 정의 · 제외 · 관점 경계를 조항으로 넣는다.
5. **personal_terms 오염 차단** — "연봉 · 조건 제시 준비 · 계획 · 근접은 합의가 아니다 (negotiating 또는 interest)" 를 추가한다.
6. **방향 규칙 보강** — "아스날이 이적의 당사자 (사는 쪽 · 파는 쪽) 가 아니면 반드시 none — 관심을 가졌거나 경쟁에서 밀린 딜도 none" 을 예시 (몽가 · 로저스) 와 함께 강화한다.
구단명은 `{club}` 파라미터로 뺀다 (§9).
7. **입력 확장** — 분류 입력을 제목 + summary_ko (1문장) 에서 제목 + summary3_ko (3문장) 로 교체한다.
둘 다 이미 저장된 필드라 생성 비용 증가는 없고 호출당 입력 토큰만 소폭 는다.
단, summary3 도 모델 생성물이라 요약 환각 (§2) 을 없애 주지는 않으므로 dry-run 에서 현행 입력판과 나란히 재서 채택한다.
본문 발췌 추가는 summary3 판이 부족할 때만 후속 검토한다.

공통 사항.

- **PLAYERS_CLAUSE 동기화** — 선수 단위 stage enum 에 done · collapsed 를 추가하고 한 줄 정의를 기사 단위와 일치시킨다 (동기화 원칙 · 가드 테스트 존재).
- **방어 불변량** — LLM 이 official 을 반환하면 강등하는 방어는 유지하되 강등 목적지를 agreed → done 으로 바꾼다 (타 매체의 "오피셜" 보도는 새 체계에서 완료 계열).
official 생성 경로는 계속 규칙 전용이다.
- **시소 중단 신호** — 문안 조정 중 한쪽을 살리면 다른 쪽이 죽는 패턴이 2판 반복되면 문구 탐색을 멈추고 근거 주입 (명단 · 메타데이터) 을 설계한다 (`docs/troubleshooting/2026-08-08-prompt-wording-seesaw-on-boundary-cases.md`).

## 5. 명단 주입의 시차 원칙

소급 재분류 시점의 명단 (예: 기마랑이스 = 완료) 이 과거 기사에도 주입되므로, "명단에 완료면 done" 으로 설계하면 사가 초반 협상 기사까지 done 으로 쏠린다.

- 1차 기준은 **기사 자체가 전하는 시점 상태**다 — 협상 보도는 협상이다.
- 명단은 "이 딜이 완료 상태임" 을 알려 후속 · 회고류 판정을 돕는 **보조 근거**로만 쓴다.
- dry-run 과잉 그룹의 사가 초반 기사 (§6) 가 이 원칙의 검증 표본이다.

## 6. dry-run 계획 (다음 실행 창 · 승인 후)

방법론은 `docs/runbook/2026-08-07-extraction-prompt-dryrun-measurement.md` 를 따른다.

- **표본 40건 안팎 · 4그룹** — 감사 판정 파일의 기대값 등록 행에서 추린다:
어긋남 12 (agreed 임박 → 협상 · 사가 후속 → 완료 · 잔류 확정 → 무산 · personal_terms "준비" → 협상) ·
과잉 경계 8 (타 구단 결말이 무산으로 오면 안 됨 · 사가 초반 기사가 완료로 쏠리면 안 됨 · 로저스 하이재킹 계열) ·
정합 10 (현행 · 새 정의 동일 판정) ·
보존 10 (타 구단 딜 단계 유지 · 방향 none 교정 · official · 가십 규칙 경로 무접촉).
- **판본 3개 × 2회** (효과 귀속 분리): v0 현행판 재현 · v1 새 프롬프트 + 현행 입력 · v2 새 프롬프트 + summary3 + 명단 주입.
배치 20건 기준 총 12호출 · 예상 비용 100원 미만 · 소요 몇 분.
- 정기 회차 (00 · 03 · 06 · 09 · 12 · 15 · 18 · 21시 KST) 사이 빈 구간에 실행하고, 판본별 결과 파일 저장 + 재개를 지원한다.
- **판정** — 어긋남 그룹 개선 폭이 같은 판 2회의 흔들림보다 클 것 · 정합 + 보존 회귀 0건.
자동 채점의 탈락 판정은 전건 원 출력으로 재확인한다 (런북 §4).

### 6.1 dry-run 결과 (2026-08-10 실행 · 12호출 · 판 3 × 2회)

하네스 · 기대값 · 판별 원 출력: `assets/2026-08-10-stage-dryrun/`.

| 그룹 | v0 현행판 | v1 새 프롬프트 | v2 새 프롬프트 + summary3 + 명단 |
| --- | --- | --- | --- |
| 어긋남 12 | 8 · 10 (현행 잣대) | **11 · 11 (새 잣대 · 2회 일치)** | 11 · 11 |
| 과잉 경계 8 | 6 · 7 | 6 · 6 | 4 · 4 |
| 정합 10 | 9 · 8 | 8 · 8 | 7 · 7 |
| 보존 10 | 10 · 9 | 8 · 8 | 8 · 8 |

- **핵심 재정의는 전부 작동** — 임박 → 협상 5/5 · 체결 완료 → 완료 · 잔류 확정 → 무산 · personal_terms "준비" 차단 · 방향 none 교정 다수 (디오망데 · 만잠비 · 토미야스 · 6aa79a9d) · 드 마샬크 스태프 인사 other 복귀.
전부 2회 반복에서 안정.
- **v2 (summary3 + 명단 주입) 는 기각** — 과잉 경계 4/8 · 정합 7/10 로 v1 보다 나쁘다.
단, 주입한 명단이 낡아 (기마랑이스 · 뇌르고르가 in_done · out_done 에 없음 — 갱신 절차 부재 · backlog d) 재료의 잠재력을 온전히 재지 못한 단서는 남긴다.
분류 입력은 현행 (제목 + summary_ko) 유지가 이번 실측의 결론이다.
- **v1 잔여 결함 = collapsed 경계 2유형** — ① 타 구단 무산 (로드리-레알) 침범 · ② "협상 거부 방침" 보도 (로메로) 를 종결로 오독.
구현 회차에 문안 1판 보강 후 재검증하고, 시소 (2판 반복) 신호가 나오면 문구 탐색을 중단한다.
완화 요인: 단계 필터가 in · out 한정 (§8) 이라 방향 none 인 침범은 화면 계수에 나타나지 않는다.
- **잔존 한계 1건** — 완결 사가의 회고 디테일 ("지불 준비가 되어 있었다" · 1bdc581a) 은 어떤 판본도 완료로 보내지 못했다.
명단 주입도 못 잡았으므로 알려진 한계로 두고 소급 후 눈검수에서 수동 처리한다.
- 하이재킹 완결 보도는 사전 등록 기대값 (주관점 규칙) 과 달리 모델이 일관되게 무산으로 판독했고, 이를 근거로 §3.2 규칙을 무산 쪽으로 재조정했다.
- v0 기준선 자체의 흔들림 (펀딧 추천 기사의 rumour ↔ interest 진동) 은 판본과 무관하게 존재 — 개정의 회귀가 아니라 기존 소음이다.

### 6.2 적용 회차 재검증 · 채택 (2026-08-10 ~ 08-11 · v1b 기각 · v1c 채택)

§6.1 의 잔여 결함 2유형을 표적으로 문안을 보강해 같은 기대값 40행 (판당 2회) 으로 재검증했다.
채점 기대값 2행은 §3.2 재조정 (하이재킹 완결 = 무산 · 방향은 시도했던 축) 을 반영해 갱신했다 — 로저스 28ae2559 · 몽가 745f1094 = collapsed · in.

- **v1b 기각 (시소 1판째)** — 정의 머리 좁힘 + "기사에 구단 미등장이면 collapsed 불가" 차단 + 방향 줄 축약을 한꺼번에 넣자, 로메로는 고쳐졌으나 비니시우스 잔류 확정 2건이 other 로 추락하는 등 회귀 5건.
잔류 판정에 필요한 "아스날 영입 대상" 근거가 입력에 없어 문구가 세계 지식 활용까지 차단한 것 — 시소 트러블슈팅 (2026-08-08) 과 같은 뿌리다.
- **v1c 채택** — v1 원문에서 두 문장만 변경: 주관점 문장 → 하이재킹 단순화 문장 (§3.2) · collapsed 정의 끝에 협상 · 매각 거부 방침 차단 한 문장.
로메로 · 하이재킹 2행 · 비니시우스 2행 · 토미야스 · 디오망데 전부 2회 반복 안정 통과.
- **알려진 한계 (사용자 확정 2026-08-11)** — v1c 의 신규 손실 2건 (로저스 회고 61f2f088 은 원제 · 요약이 서로 다른 행 · fe031baa 는 펀딧 권고의 rumour ↔ interest 진동) 은 시소 2판째 신호로 문구 탐색을 중단하고 수용했다.
근본 해법 (명단 주입) 은 명단 정리 세션의 값 갱신 뒤 재평가한다.
- **소급 후 수동 정정 목록 (눈검수 리포트에 포함)** — 로드리 a72f428a (negotiating 또는 other · none) · 로저스 회고 61f2f088 (collapsed · in) · 촐리스 1bdc581a (done · in) · 펀딧 fe031baa · d30835af (rumour · in) — 실제 소급 결과를 보고 어긋난 것만 정정한다.
- **미검증 경계 2건 (코드 리뷰 발견 · 눈검수에서 유형 확인)** — ① 아스날 자체 스쿼드 재계약 기사 (dry-run 표본에 없음 · collapsed 의 재계약 문구에 걸리면 안 되고 other 가 정답) ② 선수 단위 잔류 조항의 collapsed 개정 (PLAYERS_CLAUSE 가 구 체계의 other 지시와 모순돼 동기화했으나 소표본 미검증 — 기사-선수 어긋남 지표 재측정으로 확인).
- **재확인 사례 추가 (병렬 세션 인계 · §7 목록의 보강)** — `ef722e49` (goal · 2026-08-09 「아스날, 브루노 기마랑이스 영입 후 수비멘디에 거액 이적료 책정」) 은 아스날이 파는 건인데 방향이 in 으로 저장돼 있다.
소급 후 out 으로 교정됐는지 확인한다 — 번역 제목이 원제의 경쟁 구단 언급을 떨어뜨려 방출 신호가 약한 행이라, 방향 축 교정의 난도 표본이기도 하다.
- **과잉 경계 표본 추가 (병렬 세션 인계)** — `cd10c828` (bbc_sport · 2026-08-10 「비니시우스 영입 불발된 아스날, 대체 자원 물색 나서」) 은 무산을 전제로 깔고 대체 후보를 나열하는 두 겹 기사다.
**기대값은 interest** — 프롬프트의 "대안 후보군 · 연쇄 반응: 특정 선수를 후보로 검토한다는 보도면 interest" 조항이 이 유형의 정본이고, collapsed 는 "무산이 기사의 주된 내용일 때" 로 한정돼 전제로만 깔린 이 행에는 해당하지 않는다.
collapsed 가 나오면 신설 어휘가 전제까지 삼킨 과잉 경계 사례이므로 문구가 아니라 수동 정정으로 처리한다 (문구 탐색은 §6.2 에서 중단).
- 하네스 · 원 출력: `assets/2026-08-10-stage-dryrun/` 의 v1b_dryrun.py · v1c_dryrun.py · result_v1b_* · result_v1c_*.
- 파생 기록: 단계 필터 in · out 한정 (§8) 에 따라 방향이 none 으로 규칙 고정된 bbc_gossip 기사가 루머 계수 · 필터 목록에서 빠진다.
가십 도달 경로는 가십 밴드 · 전체 목록으로 유지된다.
- 파생 기록 2 (코드 리뷰 발견 · §8 개정으로 해소): collapsed 의 방향 분포가 none 우세라 게이트를 그대로 걸면 무산 필터가 제 내용물을 잃는다.
스펙 회차에 collapsed 방향 분포 실측이 없어 두 결정 (필터 한정 · 무산 신설) 의 충돌이 설계 단계에서 보이지 않았다.
- 파생 기록 3 (코드 리뷰 발견): 톱스토리 · 결말 카드의 단계 가중치 (`_LEAD_STAGE_RANK`) 에 신설 enum 을 넣지 않으면, 완료 보도가 미등재 (가중치 0) 로 추락하고 하이재킹 완결이 collapsed 가 되면서 결말 카드가 주 사용처에서 사라진다.
done 은 종전 agreed 자리를 잇고 collapsed 는 결말 카드 문턱 (1) 을 넘도록 편입했다.

### 6.3 소급 실행 결과 (2026-08-11 · 운영 반영 완료)

배포 (VM `a6a7020`) → 소급 → 수동 정정 → 재렌더 · 배포까지 끝났다.
백업은 VM `~/backups/articles_stage_backup_20260811_210703.sql` · 정정 로그는 같은 디렉터리의 `fixups_log.tsv`.

- **1패스 수렴** — 661행 중 가십 규칙 경로 68 · LLM 대상 593 · 호출 30회 · 2분 36초 · 429 없음 · 파싱 실패 0 · 잔존 0.
- **전후 분포** (방향 무관): 관심 186 → 188 · 협상 중 104 → 180 · 루머 140 → 95 · 이적 합의 136 → 44 · **이적 완료 0 → 40** · **무산 0 → 33** · 기타 68 → 63 · 개인 합의 10 → 3 · 메디컬 10 → 8 · 오피셜 7 → 7.
"이적 합의 > 협상 중" 역전이 해소됐다 (§7 예상치와 방향 일치 · 이적 완료는 예상 45 대비 40).
- **이동 244건 / 불변 417건** — 상위 유형은 이적 합의 → 이적 완료 32 · 이적 합의 → 협상 중 28 · 관심 → 협상 중 22 · 루머 → 관심 16 · 관심 → 무산 10.
- **알려진 사례** — 몽가 · 로저스 하이재킹은 무산 · 영입 (방향 교정 포함) · 비니시우스 재계약 4건은 기타에서 무산으로 · 트로사르 공홈 2건은 오피셜 불변 · `ef722e49` 는 방향 in → out 교정 성공 (명단 세션의 선수 축 판단과 일치) · `cd10c828` 은 기대대로 관심 유지 (무산 어휘에 안 끌림).
- **수동 정정 14건** — 프롬프트 조항을 명시적으로 어긴 행만 손댔다: 이적 산업 칼럼 1 · 타 구단 딜 무산이 무산으로 3 · 대안 후보군이 무산으로 1 · 완료 아님이 이적 완료로 3 · 근황 기사 1 · 스펙 등록 한계 2 (촐리스 회고 · 로저스 회고) · 복합 기사 1 · 방향만 2.
판단이 갈리는 3건 (외데고르 이적설 부인 · 무리뉴 발언 · 토미야스 소속 시점) 은 손대지 않았다.
- **무산의 방향 예외가 값을 했다** — 무산 33건 중 방향이 없는 것이 8건이라, §8 개정이 없었으면 그만큼이 필터에서 빠졌을 것이다 (비니시우스 재계약 계열이 여기 포함).
- **표시 계층 잔여 문제와 그 해소** — 선수 페이지 사다리는 `article_players.stage` 를 읽어 처음에는 옛 어휘로 남았고, 같은 기사가 카드에서는 무산 · 선수 페이지에서는 이적 합의로 보였다 (실측 72건 · 경위는 `docs/troubleshooting/2026-08-11-article-axis-only-reclassification-splits-the-screen.md`).
사용자 승인으로 같은 회차에 선수 축 재추출을 이어 돌려 해소했다 (§6.4).

### 6.4 선수 축 재추출 (2026-08-11 · 같은 회차 후속)

기사 축만 고치면 선수 페이지가 옛 어휘로 남는 문제를 그 자리에서 닫았다.

- **대상은 화면에 어긋남이 보이는 63건** — 선수 축이 기타인 귀속은 선수 페이지 목록에서 어차피 빠지므로 제외했다.
기존 `reextract_article_players` 를 그대로 쓰고, state 파일에 나머지 연결 기사 516건을 미리 넣어 대상만 남겼다 (새 코드 없음).
- 결과: 처리 63/63 · 호출 64회 · 66초 · 429 없음 · 재삽입 실패 0 · 고아 귀속 21쌍 삭제 · 신규 후보 1명 등재.
백업은 VM `~/backups/article_players_backup_20260811_213323.sql`.
- **두 축 정렬** — 기사 축이 이적 완료 · 무산인 기사의 선수 축에 이적 완료 41 · 무산 27 이 생겼고, 어긋나 있던 이적 합의는 49 → 7 로 줄었다.
남은 불일치는 한 기사가 여러 선수를 다룰 때 축이 갈리는 정상 범위다.
- **비니시우스 페이지에 종결 줄이 나왔다** — 저장소 최초 사례다 (진행 축 아래 점선 · 「레알과 재계약 합의」 · 같은 단계 10건).
- **CDN 캐시 함정** — 배포 직후 라이브 주소로 받으면 옛 페이지가 온다.
VM 의 `site/` 산출물에는 이미 반영돼 있었는데 라이브만 캐시였다.
배포별 주소 (`https://<배포id>.bullet-in.pages.dev/...`) 로 받으면 즉시 새 것이 온다 — "반영이 안 됐다" 로 오진하기 쉽다.

#### 6.4.1 재추출이 드러낸 것 (병렬 세션 인계 · 실측 확인)

- **명단 축이 낡은 선수 4명이 드러났다** — 토날리 · 코네 · 고든 · 알바레스.
선수 축에 종결 단계가 붙으면서 명단 축 낡음 관측 알림 (#246) 의 종결 방아쇠가 처음 걸렸고, 토날리는 참 발화로 확인됐다 (본문 근거 있음).
재추출이 알림의 검출력을 실제로 살린 셈이다.
- **알려진 경계 (다음 소표본 재검증 대상)** — 여러 이탈자를 나열한 문장에서 타 구단 간 완료 이적이 선수 축 `done` 으로 붙는 경향이 있다.
`83f84ae9` (「기마랑이스, 아스날 이적 원했다」) 에서 토날리 · 고든이 done 을 받았는데, 선수 축은 아스날 기준이므로 아스날 건의 진행을 말하지 않는 나열은 기타가 맞다.
규모는 작다 — 선수 축 종결 단계 76행 중 명단 축과 어긋나는 조합은 10행 안팎이고, 그중 대부분은 오분류가 아니라 명단이 낡은 것이었다.
이번 회차는 문구 탐색을 중단한 상태이므로 (§6.2) 고치지 않고 다음 개정의 소표본에 이 유형을 넣는다.
- **알려진 경계 2 (같은 이유로 다음 회차)** — 합의 보도를 하이재킹 완결로 읽는 경로가 있다.
`79b99b1b` (「맨유, 아스날 제치고 코네 영입 합의」) 의 코네 선수 축이 무산인데, 사용자 확인 결과 그 이적은 아직 성사되지 않았다.
무산은 딜의 종결이고 하이재킹은 완결이 요건이므로 (§3.2) 합의 단계에서 종결로 보내면 이르다.

### 6.5 화면 실측에서 나온 표시 규칙 정정 (2026-08-11 · 사용자 지적)

소급 · 재추출 뒤 선수 페이지를 실제로 열어 보고 네 가지를 지적받았다.
세 가지는 표시 규칙 문제라 같은 회차에 코드로 고쳤고, 하나는 추출 문제라 다음 회차로 넘겼다.

- **현재 단계가 종결을 우선한다 (사다리 스펙 §6.2 개정)** — 머리 · 색인 카드의 단계 배지는 시간축 최신값이었는데, 딜이 끝난 뒤 그 선수를 배경으로만 언급한 기사가 뒤에 오면 그 기사의 단계가 현재 상태를 덮었다.
재계약으로 끝난 비니시우스가 협상 중으로 표시된 것이 실물이다.
이제 종결 단계 (이적 완료 · 무산) 가 있으면 가장 늦은 종결을 현재 상태로 삼는다.
**오피셜은 종결에 넣지 않는다** — 공홈은 합의 때 · 확정 때 각각 올라오고, 사다리 첫 줄을 현재 상태로 읽으면 실측 다섯 명이 전부 틀렸던 선례가 있다 (사다리 스펙 §6.2 의 원래 근거).
- **종결 줄을 사다리 맨 위로 (§8 개정)** — 진행 축 아래에 두면 끝난 사가가 아직 진행 중인 것처럼 읽힌다.
사다리는 위가 가장 진행된 단계이므로 종결도 위에 두고, 점선으로 진행 축과 가른다.
- **색인에서 값이 하나뿐인 그룹의 축 배지 생략** — 타 클럽행 그룹은 배지가 그룹명을 되풀이하고, 이적 무산 그룹은 같은 값을 "링크 소멸" 이라는 다른 말로 불렀다.
그룹 안에서 가릴 것이 없으므로 색인에서는 배지를 뺀다 (진행 중 · 이적 확정 그룹은 값이 여럿이라 그대로 둔다).
선수 페이지는 그룹 맥락이 없어 배지를 유지한다.
- **종결은 명단 축이 뒷받침할 때만 쓴다 (같은 회차 재정정)** — 위 개정을 화면 전체에 대조해 보니 종결 우선 규칙이 상류 오분류 한 건을 배지에 영구히 박았다.
영입을 마친 업슨이 무산으로, 아직 이적하지 않은 코네 · 알바레스가 무산으로 뒤집혔다 (세 건 모두 기사 축 오분류가 원인).
명단 축은 사람이 확정한 현재 상태라 모델이 낸 기사 단계보다 믿을 만하므로, 종결은 명단 축이 뒷받침할 때만 쓰고 부정하면 최신값 경로에서도 뺀다.
결과: 54명 중 배지가 바뀌는 선수 7명 · 두 배지가 서로 모순되는 경우 0건.
- **이적을 마친 선수는 공홈 발표를 먼저 쓴다** — 완료 기사 유무에 따라 어떤 선수는 오피셜 · 어떤 선수는 이적 완료로 갈렸다 (실측: 완료 9명 중 6명이 공홈 발표 보유 · 키비오르만 완료 기사가 0건이라 오피셜이 남았다).
명단 축 뒷받침 조건이 있어 진행 중인 선수의 공홈 합의 공지가 현재 상태를 가로채던 옛 문제는 재발하지 않는다.
- **선수 페이지의 카드 배지를 선수 축으로** — 카드는 기사 축, 사다리는 선수 축이라 한 페이지에서 두 어휘가 섞였다.
아스날 건이 관심에서 멈춘 스톤스의 카드에 인터 밀란과의 이적 합의가 뜨던 자리다.
목록 · 상세는 기사 축을 그대로 쓴다 — 거기서는 그 기사가 다루는 딜의 단계가 맞다.
- **사다리 대표는 그 선수를 다룬 기사를 우선한다** — 대표를 공신력만으로 뽑으면 오귀속 기사가 본인 기사를 이긴다.
뇌르고르 사다리 세 줄이 전부 기마랑이스 기사였고, 오귀속 쪽이 공신력 최상이었다.
제목에 그 선수 이름 (성) 이 있는 기사를 먼저 고르는 것으로 완화했다 — 귀속 자체를 좁히는 것은 추출 몫이다.
- **귀속 과다는 다음 회차** — 주역이 아닌 기사가 선수 페이지에 쌓인다.
비니시우스는 귀속 97건 중 제목에 이름이 없는 것이 14건이고, 앤서니 고든 페이지는 유일한 기사가 기마랑이스 기사다 (나열 문장에 이름만 등장).
재추출로도 좁혀지지 않았다 — `f261f980` 은 재추출 대상에 들어 있었는데 귀속이 유지됐다.
추출 프롬프트 문제라 소표본 재검증이 필요하고, §6.4.1 의 두 경계와 함께 다음 개정으로 넘긴다.

## 7. 소급 재분류 계획 (다음 회차)

- 대상: 분류 전건 약 606건 — **단계와 방향을 함께** NULL 복원 → 분류 패스 재실행 (기존 멱등 런북 경로 · 최대 3패스 수렴 · fetch 없음).
호출 약 35 ~ 40회 · 비용 수백 원 미만 — 단 Gemini Tier 1 선불이므로 실행 전 승인을 받는다.
- 선행: DB 백업 덤프 · before 분포 재측정.
- 검증: 전후 분포 비교표 + 이적 합의 · 완료 · 무산 전건 제목 눈검수 리포트 + 알려진 사례 재확인:
몽가 745f1094 · 로저스 하이재킹 28ae2559 (무산 · in — §3.2 재조정 규칙) · 노르가르 표기 2건 (방향 out) · 비니시우스 잔류 4건 (무산) · 드 마샬크 2건 (기타) · 트로사르 공홈 2건 (오피셜 유지) · 기마랑이스 사가 3층 분해 (§3.1) · 토미야스 2건 (방향 none).
- 재분류 후 재렌더 · 표시 계층 확인까지가 완료 조건이다.
- 예상 분포 (agreed 표본 20건 환산 · 참고치): agreed 121 → 이적 합의 약 30 · 협상 중 +35 · 완료 약 45 · 무산 · 기타 소수.
"이적 합의 > 협상 중" 역전이 구조적으로 해소된다.

## 8. 표시 계층 설계 (구현은 다음 회차 · serve 필드 조율)

- **단계 필터 in · out 한정** — 사이드바 단계 필터의 계수와 목록을 방향 in · out (아스날 주체) 기사로 한정한다.
타 구단 딜 (none) 은 DB 분류를 유지한 채 필터 분모에서만 빠지고, 전체 목록 · 팀 facet (구단 칩) · 사건 묶음 · 선수 필터로는 계속 도달된다.
이 설계에서는 방향 값이 필터 노출을 좌우하므로 **방향 재분류 (§7) 의 정확도가 선행 조건**이다.
- **무산은 방향 게이트의 예외다 (개정 2026-08-11 · 사용자 확정)** — collapsed 는 정의상 아스날 관련 딜의 종결에만 붙는 관점 값이라 "타 구단 딜 제외" 라는 한정의 취지가 분류 단계에서 이미 충족돼 있다.
방향까지 걸면 잔류 확정 · 재계약 체결 (dry-run 실측상 방향 none · 하이재킹만 in) 이 자기 필터에서 빠져, 카드에는 무산 배지가 보이는데 사이드바 건수는 0 에 가깝고 필터를 체크하면 사라지는 불일치가 생긴다.
무산 신설의 동기 자체가 "사가 결말이 기본 숨김 버킷으로 사라진다" 였으므로 필터 도달 가능성을 우선한다.
대가로 타 구단 무산 침범 (§6.1 의 로드리 유형) 이 필터에 노출되므로 눈검수 · 수동 정정으로 관리한다.
판정은 `render.in_stage_filter()` 와 app.js 가 같은 규칙을 공유한다.
- **사건 묶음 · 클럽 칩과 독립** — 카드 배지 (단계) · 클럽 칩 (팀 축) · 묶음 소그룹 (이벤트 클러스터링) 은 별개 기계로 불변이다.
재분류 후에는 같은 묶음 안에서 타 구단 성사 = 이적 합의 → 완료, 아스날 쪽 = 무산으로 갈라져 묶음의 이야기 구조가 선명해진다.
- **사다리와의 관계** — 사다리 정렬 기준은 `render._STAGE_DISPLAY_GROUPS` 순서다 (2026-08-05 사다리 스펙 §4.1).
완료는 오피셜 아래 진행 최상단에 삽입하고, **무산은 진행 단계가 아니므로 사다리 축에 넣지 않고 종결 표시로 처리**한다.
구체 화면은 다음 회차 serve 세션 몫이다.

## 9. 멀티 클럽 확장 대비

빅6 확장 의향 (사용자 · 2026-08-01) 을 고려한 제약을 명시한다.

- 단계 축은 딜 자체의 속성이라 확장에 안전하다.
- **방향 축과 무산은 관점 값이다** — 같은 기사가 첼시 관점에선 in/완료, 아스날 관점에선 none/무산이다.
단일 컬럼 방향은 멀티 클럽에서 구조적으로 깨지므로, 확장 착수 시 구단별 관점 분리 (관점 테이블 또는 구단 주입 재분류) 가 선행이다.
- 이번 개정에서는 프롬프트의 구단명을 `{club}` 파라미터로 빼 두는 것까지만 한다 — 확장 시 프롬프트 재작성 없이 구단명 주입으로 간다.
"아스날 = 전부" 하드코딩 경계 원칙과 같은 줄기다.
- **확장 착수 시 무산 · 방향은 대상 전 구단에 대해 재도출한다** — collapsed 행이 재도출 1순위다.
하이재킹 무산 규칙 (§3.2) 을 골라도 관점 오염이 새로 생기는 것이 아니다 — 잔류 확정 (타 구단의 재계약 성사) 같은 무산 값 자체가 태생적으로 관점 값이라, 어느 규칙이든 확장 시 재도출은 같은 크기로 필요하다.
- 확장 전까지 `transfer_stage` 의 collapsed 와 `transfer_direction` 은 **아스날 관점 값**이다.
다른 소비자 (분석 · 알림) 가 구단 중립 값으로 오용하지 않도록 스키마 주석 수준으로 명시한다 (구현 회차).

## 10. 테스트 (구현 회차에서)

- `SIDEBAR_STAGES` 에 done · collapsed 추가 (css 2색 신규) · `_STAGE_DISPLAY_GROUPS` 개편 (메디컬 편입 · 완료 · 무산 그룹).
- 동기화 가드 갱신 — LLM enum 집합 = VALID_STAGES − {official} + {done, collapsed} · STAGE_PROMPT ↔ PLAYERS_CLAUSE 정의 일치.
- official 강등 목적지 done 변경 테스트 · rule_stage 2경로 (공홈 · 가십) 불변 테스트.
- 429 즉시 중단 · 파싱 실패 스킵 · 미허용값 other 강등은 기존 규칙 · 테스트 유지.
- dbt 는 transfer_stage 게이트 없음 (07-19 실측) — 구현 시 재확인만 한다.

## 11. 범위 밖

- serve 구현 · 소급 실행 · 재렌더 — 다음 회차 (병렬 세션과 serve 필드 조율).
- 수집 경로 명단 배선 코드 (backlog c) — 이 스펙이 단계 정의를 제공하는 선행 관계만 명시.
- 링크 축 자동 감지 알림 (backlog d) · 멀티 클럽 구현 (§9 의 제약 명시까지만).
- 제목 오역 (ce42d34d) — 번역 신뢰성 필드 소관.
- 뇌르고르 / 노르가르 표기 통일 — roster 데이터 몫 (명단 정리 세션에 인계).

## 12. 참조

- 선행 확정: `docs/superpowers/specs/2026-07-19-transfer-stage-overhaul-design.md` (이번 스펙이 §2 를 재개정)
- 방향 축: `docs/superpowers/specs/2026-08-02-transfer-stage-direction-design.md`
- 시소 중단 신호: `docs/troubleshooting/2026-08-08-prompt-wording-seesaw-on-boundary-cases.md`
- dry-run 방법론: `docs/runbook/2026-08-07-extraction-prompt-dryrun-measurement.md`
- 사다리 정렬: `docs/superpowers/specs/2026-08-05-serve-player-progress-ladder-design.md`
- 감사 판정 파일: `docs/superpowers/specs/assets/2026-08-10-article-stage-audit-judgments.tsv`
