# Bullet-in 구현 계획 (v1)

> **에이전트 작업자용:** 필수 하위 스킬 — 이 계획을 태스크 단위로 구현하려면 superpowers:subagent-driven-development (권장) 또는 superpowers:executing-plans를 사용하세요. 각 단계는 체크박스 (`- [ ]`) 형식으로 추적합니다.

**목표:** 매일 아스날 FC 뉴스를 이종 (異種) 소스에서 병렬 수집하고, 중복제거 · 공신력 스코어링 · LLM 번역/요약 · 데이터 품질 게이트를 거쳐 얇은 정적 HTML로 서빙하는 파이프라인을 만든다. 오케스트레이션은 Airflow (2.x 구축 후 3.x 마이그레이션)로 한다.

**아키텍처:** 메달리온 파이프라인. 단일 `SourceAdapter` 인터페이스가 RSS / Guardian API / 정적 HTML / Playwright / X (twikit)에 대해 asyncio로 팬아웃 수집한다. 원문은 MongoDB에 불변 적재하고, 정규화 · 중복제거 · 스코어링 · 번역된 행은 MariaDB 서빙 마트로 보낸다. dbt (DuckDB가 MariaDB를 attach)가 분석 마트를 빌드하고 테스트를 품질 게이트로 실행한다. Jinja2가 정적 HTML을 렌더링하고, Airflow가 전 단계를 조율한다.

**기술 스택:** Python 3.11, `uv`, pydantic v2, httpx, feedparser, Playwright (async), twikit, pymongo, SQLAlchemy 2.0 + PyMySQL, dbt-duckdb, anthropic SDK (claude-haiku-4-5), Jinja2, tenacity, structlog, pytest + pytest-asyncio. MongoDB/MariaDB/Airflow는 Docker Compose.

---

## 확정된 구현 결정사항

엔지니어가 추측하지 않도록 다음을 못박는다:

- **패키지/환경 관리:** `pyproject.toml` 기반 `uv`. 소스 레이아웃은 `src/bullet_in/`.
- **v1 구체 소스:** Arsenal.com (RSS, 없으면 HTML), Guardian Open Platform API, BBC Sport (정적 HTML), Goal.com (Playwright — JS 렌더링, Playwright 필수 시연 지점), ITK X 계정 `handofarsnal`(twikit), football.london (Tier 4 정적).
- **LLM:** Anthropic `claude-haiku-4-5`, 공식 `anthropic` SDK 사용. 키는 `.env`의 `ANTHROPIC_API_KEY`.
- **MariaDB 드라이버:** SQLAlchemy 2.0 + `PyMySQL`.
- **테스트 DB:** 순수 로직은 DB 불필요, MongoDB 단위테스트는 `mongomock`, MariaDB 통합테스트는 Docker 컨테이너의 `bulletin_test` 스키마.
- **Airflow:** `2.9.x`로 구축 후 `3.0.x`로 마이그레이션. breaking change 대응은 `docs/MIGRATION.md`에 기록.

---

## 파일 구조

| 경로 | 책임 |
|---|---|
| `pyproject.toml` | 의존성, 도구 설정, pytest 설정 |
| `docker-compose.yml` | MongoDB, MariaDB, (이후) Airflow 서비스 |
| `.env.example` | 필요한 환경변수 이름 (시크릿 없음) |
| `config/sources.yaml` | 소스 → tier/medium/adapter 매핑 (source of truth) |
| `src/bullet_in/models.py` | pydantic `RawItem`, `Article` 스키마 |
| `src/bullet_in/canonical.py` | URL 정규화 + `content_hash` |
| `src/bullet_in/dedup.py` | 신규/중복 판정, 변경 감지 |
| `src/bullet_in/score.py` | `sources.yaml` 기반 tier/confidence 스코어링 |
| `src/bullet_in/adapters/base.py` | `SourceAdapter` 프로토콜 + `RawItem` 계약 |
| `src/bullet_in/adapters/rss.py` | RSS 어댑터 (feedparser) |
| `src/bullet_in/adapters/guardian_api.py` | Guardian Open Platform API 어댑터 |
| `src/bullet_in/adapters/html.py` | 정적 HTML 어댑터 (httpx + 파서) |
| `src/bullet_in/adapters/playwright_news.py` | JS 렌더링 어댑터 (async Playwright) |
| `src/bullet_in/adapters/x_twikit.py` | X ITK 어댑터 (twikit) |
| `src/bullet_in/ingest.py` | asyncio 팬아웃 + 소스별 격리 |
| `src/bullet_in/storage/mongo.py` | 원문 랜딩 저장소 |
| `src/bullet_in/storage/mariadb.py` | 마트 저장소: upsert, 워터마크, 런 기록 |
| `src/bullet_in/enrich.py` | LLM 번역 + 요약 (멱등) |
| `src/bullet_in/quality.py` | 런타임 이상탐지 + SLO 집계 |
| `src/bullet_in/serve/render.py` | Jinja2 → 정적 HTML |
| `src/bullet_in/serve/templates/index.html.j2` | 페이지 템플릿 |
| `src/bullet_in/metrics.py` | 병렬 speedup 벤치마크 하니스 |
| `dbt/` | dbt-duckdb 프로젝트: staging + marts + tests |
| `airflow/dags/bullet_in_daily.py` | 일간 DAG (2.x → 3.x) |
| `docs/MIGRATION.md`, `docs/troubleshooting/`, `docs/runbook/` | 운영 문서 |
| `tests/` | `src/`를 미러링하는 pytest 스위트 |

---

## Phase 0 — 프로젝트 스캐폴딩

### Task 0.1: 프로젝트 및 의존성 초기화

**파일:**
- 생성: `pyproject.toml`, `src/bullet_in/__init__.py`, `tests/__init__.py`

- [ ] **Step 1: `pyproject.toml` 생성**

```toml
[project]
name = "bullet-in"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
  "pydantic>=2.6",
  "httpx>=0.27",
  "feedparser>=6.0",
  "playwright>=1.44",
  "twikit>=2.0",
  "pymongo>=4.7",
  "sqlalchemy>=2.0",
  "pymysql>=1.1",
  "anthropic>=0.40",
  "jinja2>=3.1",
  "tenacity>=8.3",
  "structlog>=24.1",
  "pyyaml>=6.0",
]

[project.optional-dependencies]
dev = ["pytest>=8.2", "pytest-asyncio>=0.23", "respx>=0.21", "mongomock>=4.1", "dbt-duckdb>=1.8"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/bullet_in"]
```

- [ ] **Step 2: 패키지 마커 생성**

빈 `src/bullet_in/__init__.py`와 `tests/__init__.py`를 만든다.

- [ ] **Step 3: 설치**

실행: `uv sync --extra dev && uv run playwright install chromium`
기대: 환경 의존성 해결, chromium 다운로드 완료.

- [ ] **Step 4: pytest 동작 확인 (아직 테스트 없음)**

실행: `uv run pytest -q`
기대: "no tests ran" (exit 5) — 하니스 동작 확인.

- [ ] **Step 5: 커밋**

```bash
git add pyproject.toml src/bullet_in/__init__.py tests/__init__.py
git commit -m "chore: scaffold project and dependencies"
```

### Task 0.2: 데이터 스토어용 Docker Compose

**파일:**
- 생성: `docker-compose.yml`, `.env.example`

- [ ] **Step 1: `docker-compose.yml` 생성**

```yaml
services:
  mongo:
    image: mongo:7
    ports: ["27017:27017"]
    volumes: ["mongo_data:/data/db"]
  mariadb:
    image: mariadb:11
    environment:
      MARIADB_ROOT_PASSWORD: bulletin
      MARIADB_DATABASE: bulletin
    ports: ["3306:3306"]
    volumes: ["maria_data:/var/lib/mysql"]
volumes:
  mongo_data:
  maria_data:
```

- [ ] **Step 2: `.env.example` 생성**

```
MONGO_URI=mongodb://localhost:27017
MONGO_DB=bulletin
MARIADB_URL=mysql+pymysql://root:bulletin@localhost:3306/bulletin
ANTHROPIC_API_KEY=sk-ant-REPLACE
GUARDIAN_API_KEY=REPLACE
X_USERNAME=REPLACE
X_EMAIL=REPLACE
X_PASSWORD=REPLACE
```

- [ ] **Step 3: 스토어 기동 및 확인**

실행: `docker compose up -d && docker compose ps`
기대: `mongo`, `mariadb` 모두 `running`/healthy.

- [ ] **Step 4: 커밋**

```bash
git add docker-compose.yml .env.example
git commit -m "chore: add docker-compose for mongo and mariadb"
```

### Task 0.3: 소스 설정

**파일:**
- 생성: `config/sources.yaml`

- [ ] **Step 1: `config/sources.yaml` 생성**

```yaml
sources:
  - source_id: arsenal_official
    display_name: Arsenal.com
    tier: 0
    medium: official
    adapter: rss
    config: { feed_url: "https://www.arsenal.com/rss-feeds/news" }
    enabled: true
  - source_id: guardian
    display_name: The Guardian
    tier: 1.5
    medium: newspaper
    adapter: guardian_api
    config: { query: "Arsenal", section: "football" }
    enabled: true
  - source_id: bbc_sport
    display_name: BBC Sport
    tier: 2
    medium: newspaper
    adapter: html
    config:
      list_url: "https://www.bbc.com/sport/football/teams/arsenal"
      item_selector: "a[href*='/sport/football/articles/']"
    enabled: true
  - source_id: goal
    display_name: Goal.com
    tier: 2
    medium: newspaper
    adapter: playwright
    config:
      list_url: "https://www.goal.com/en/team/arsenal/news/4dsgumo7d4ztgmgmcwzj0scnv"
      item_selector: "a[data-testid='article-link']"
    enabled: true
  - source_id: football_london
    display_name: football.london
    tier: 4
    medium: newspaper
    adapter: html
    config:
      list_url: "https://www.football.london/arsenal-fc/"
      item_selector: "a.headline"
    enabled: true
  - source_id: x_handofarsnal
    display_name: handofarsnal (ITK)
    tier: 1.5
    medium: x
    adapter: x_twikit
    config: { handle: "handofarsnal", max_tweets: 20 }
    enabled: true
```

> 주의: `item_selector`, `feed_url` 값은 DOM 의존적이므로 해당 어댑터 태스크에서 실제 사이트로 확인해 갱신한다. 여기서가 아니라 거기서 고친다.

- [ ] **Step 2: 커밋**

```bash
git add config/sources.yaml
git commit -m "feat: add source/tier configuration"
```

---

## Phase 1 — 핵심 도메인 로직 (TDD)

### Task 1.1: 데이터 모델

**파일:**
- 생성: `src/bullet_in/models.py`
- 테스트: `tests/test_models.py`

- [ ] **Step 1: 실패하는 테스트 작성**

```python
# tests/test_models.py
from datetime import datetime, timezone
from bullet_in.models import RawItem, Article

def test_rawitem_requires_core_fields():
    item = RawItem(source_id="guardian", source_type="api",
                   url="https://x.test/a", fetched_at=datetime.now(timezone.utc),
                   raw_payload={"k": "v"})
    assert item.url == "https://x.test/a"

def test_article_defaults_enrichment_to_none():
    art = Article(content_hash="abc", url="https://x.test/a",
                  source_id="guardian", title_original="Title",
                  published_at=datetime.now(timezone.utc))
    assert art.title_ko is None and art.summary_ko is None and art.revision == 1
```

- [ ] **Step 2: 실행해서 실패 확인**

실행: `uv run pytest tests/test_models.py -v`
기대: FAIL — `ModuleNotFoundError: bullet_in.models`.

- [ ] **Step 3: 구현**

```python
# src/bullet_in/models.py
from __future__ import annotations
from datetime import datetime
from typing import Any, Literal
from pydantic import BaseModel

SourceType = Literal["rss", "api", "html", "playwright", "x"]

class RawItem(BaseModel):
    source_id: str
    source_type: SourceType
    url: str
    fetched_at: datetime
    raw_payload: dict[str, Any]
    content_hash: str | None = None

class Article(BaseModel):
    content_hash: str
    url: str
    source_id: str
    author: str | None = None
    tier: float | None = None
    confidence_score: float | None = None
    title_original: str
    title_ko: str | None = None
    summary_ko: str | None = None
    body_excerpt: str | None = None
    published_at: datetime
    revision: int = 1
```

- [ ] **Step 4: 실행해서 통과 확인**

실행: `uv run pytest tests/test_models.py -v`
기대: PASS (2 passed).

- [ ] **Step 5: 커밋**

```bash
git add src/bullet_in/models.py tests/test_models.py
git commit -m "feat: add RawItem and Article models"
```

### Task 1.2: URL 정규화 + content hash

**파일:**
- 생성: `src/bullet_in/canonical.py`
- 테스트: `tests/test_canonical.py`

- [ ] **Step 1: 실패하는 테스트 작성**

```python
# tests/test_canonical.py
from bullet_in.canonical import canonical_url, content_hash

def test_canonical_strips_tracking_and_fragment():
    a = canonical_url("https://x.test/a?utm_source=tw&id=5#frag")
    assert a == "https://x.test/a?id=5"

def test_canonical_lowercases_host_and_drops_trailing_slash():
    assert canonical_url("https://X.Test/a/") == "https://x.test/a"

def test_content_hash_stable_and_title_insensitive_to_whitespace():
    h1 = content_hash("  Arteta speaks  ", "https://x.test/a")
    h2 = content_hash("Arteta speaks", "https://x.test/a")
    assert h1 == h2 and len(h1) == 64
```

- [ ] **Step 2: 실행해서 실패 확인**

실행: `uv run pytest tests/test_canonical.py -v`
기대: FAIL — 모듈 없음.

- [ ] **Step 3: 구현**

```python
# src/bullet_in/canonical.py
import hashlib
from urllib.parse import urlsplit, urlunsplit, parse_qsl, urlencode

_TRACKING_PREFIXES = ("utm_", "fbclid", "gclid")

def canonical_url(url: str) -> str:
    parts = urlsplit(url.strip())
    host = parts.netloc.lower()
    path = parts.path.rstrip("/") or "/"
    if path == "/":
        path = ""
    kept = [(k, v) for k, v in parse_qsl(parts.query)
            if not any(k.lower().startswith(p) for p in _TRACKING_PREFIXES)]
    query = urlencode(sorted(kept))
    return urlunsplit((parts.scheme, host, path, query, ""))

def content_hash(title: str, url: str) -> str:
    norm_title = " ".join(title.split())
    payload = f"{norm_title}|{canonical_url(url)}".encode("utf-8")
    return hashlib.sha256(payload).hexdigest()
```

- [ ] **Step 4: 실행해서 통과 확인**

실행: `uv run pytest tests/test_canonical.py -v`
기대: PASS (3 passed).

- [ ] **Step 5: 커밋**

```bash
git add src/bullet_in/canonical.py tests/test_canonical.py
git commit -m "feat: add URL canonicalization and content hashing"
```

### Task 1.3: 중복제거 + 변경 감지

**파일:**
- 생성: `src/bullet_in/dedup.py`
- 테스트: `tests/test_dedup.py`

- [ ] **Step 1: 실패하는 테스트 작성**

```python
# tests/test_dedup.py
from bullet_in.dedup import classify

def test_new_when_url_unseen():
    assert classify(url="https://x.test/a", new_hash="h1", seen={}) == ("new", 1)

def test_duplicate_when_url_and_hash_match():
    assert classify(url="https://x.test/a", new_hash="h1",
                    seen={"https://x.test/a": ("h1", 3)}) == ("duplicate", 3)

def test_changed_when_url_seen_but_hash_differs():
    assert classify(url="https://x.test/a", new_hash="h2",
                    seen={"https://x.test/a": ("h1", 3)}) == ("changed", 4)
```

- [ ] **Step 2: 실행해서 실패 확인**

실행: `uv run pytest tests/test_dedup.py -v`
기대: FAIL — 모듈 없음.

- [ ] **Step 3: 구현**

```python
# src/bullet_in/dedup.py
from typing import Literal

Decision = Literal["new", "duplicate", "changed"]

def classify(url: str, new_hash: str,
             seen: dict[str, tuple[str, int]]) -> tuple[Decision, int]:
    """seen: canonical url -> (last_hash, last_revision)."""
    if url not in seen:
        return "new", 1
    last_hash, last_rev = seen[url]
    if last_hash == new_hash:
        return "duplicate", last_rev
    return "changed", last_rev + 1
```

- [ ] **Step 4: 실행해서 통과 확인**

실행: `uv run pytest tests/test_dedup.py -v`
기대: PASS (3 passed).

- [ ] **Step 5: 커밋**

```bash
git add src/bullet_in/dedup.py tests/test_dedup.py
git commit -m "feat: add dedup and change-detection logic"
```

### Task 1.4: 공신력 스코어링

**파일:**
- 생성: `src/bullet_in/score.py`
- 테스트: `tests/test_score.py`, `tests/fixtures/sources_min.yaml`

- [ ] **Step 1: 픽스처 `tests/fixtures/sources_min.yaml` 생성**

```yaml
sources:
  - source_id: arsenal_official
    tier: 0
    enabled: true
  - source_id: football_london
    tier: 4
    enabled: true
  - source_id: disabled_one
    tier: 2
    enabled: false
```

- [ ] **Step 2: 실패하는 테스트 작성**

```python
# tests/test_score.py
from pathlib import Path
from bullet_in.score import load_sources, confidence

FIX = Path(__file__).parent / "fixtures" / "sources_min.yaml"

def test_load_only_enabled_sources():
    s = load_sources(FIX)
    assert set(s) == {"arsenal_official", "football_london"}

def test_confidence_is_higher_for_lower_tier():
    s = load_sources(FIX)
    assert confidence("arsenal_official", s) > confidence("football_london", s)

def test_unknown_source_gets_floor_confidence():
    s = load_sources(FIX)
    assert confidence("who_dis", s) == 0.0
```

- [ ] **Step 3: 실행해서 실패 확인**

실행: `uv run pytest tests/test_score.py -v`
기대: FAIL — 모듈 없음.

- [ ] **Step 4: 구현**

```python
# src/bullet_in/score.py
from pathlib import Path
import yaml

def load_sources(path: str | Path) -> dict[str, dict]:
    data = yaml.safe_load(Path(path).read_text(encoding="utf-8"))
    return {s["source_id"]: s for s in data["sources"] if s.get("enabled", True)}

def confidence(source_id: str, sources: dict[str, dict]) -> float:
    """tier 0..4 를 confidence 1.0..0.0 로 선형 매핑. 미지의 소스는 0.0."""
    src = sources.get(source_id)
    if src is None:
        return 0.0
    tier = float(src["tier"])
    return round(max(0.0, 1.0 - tier / 4.0), 3)
```

- [ ] **Step 5: 실행해서 통과 확인**

실행: `uv run pytest tests/test_score.py -v`
기대: PASS (3 passed).

- [ ] **Step 6: 커밋**

```bash
git add src/bullet_in/score.py tests/test_score.py tests/fixtures/sources_min.yaml
git commit -m "feat: add credibility scoring from sources config"
```

---

## Phase 2 — 소스 어댑터 & 병렬 수집

### Task 2.1: 어댑터 기본 계약

**파일:**
- 생성: `src/bullet_in/adapters/__init__.py`, `src/bullet_in/adapters/base.py`
- 테스트: `tests/test_adapter_base.py`

- [ ] **Step 1: 실패하는 테스트 작성**

```python
# tests/test_adapter_base.py
import asyncio
from datetime import datetime, timezone
from bullet_in.adapters.base import SourceAdapter
from bullet_in.models import RawItem

class FakeAdapter:
    source_id = "fake"
    source_type = "api"
    async def fetch(self) -> list[RawItem]:
        return [RawItem(source_id="fake", source_type="api", url="https://x.test/a",
                        fetched_at=datetime.now(timezone.utc), raw_payload={})]

def test_fake_adapter_satisfies_protocol():
    a = FakeAdapter()
    assert isinstance(a, SourceAdapter)
    items = asyncio.run(a.fetch())
    assert items[0].source_id == "fake"
```

- [ ] **Step 2: 실행해서 실패 확인**

실행: `uv run pytest tests/test_adapter_base.py -v`
기대: FAIL — 모듈 없음.

- [ ] **Step 3: 구현**

```python
# src/bullet_in/adapters/__init__.py
```

```python
# src/bullet_in/adapters/base.py
from __future__ import annotations
from typing import Protocol, runtime_checkable
from bullet_in.models import RawItem

@runtime_checkable
class SourceAdapter(Protocol):
    source_id: str
    source_type: str
    async def fetch(self) -> list[RawItem]: ...
```

- [ ] **Step 4: 실행해서 통과 확인**

실행: `uv run pytest tests/test_adapter_base.py -v`
기대: PASS.

- [ ] **Step 5: 커밋**

```bash
git add src/bullet_in/adapters/__init__.py src/bullet_in/adapters/base.py tests/test_adapter_base.py
git commit -m "feat: add SourceAdapter protocol"
```

### Task 2.2: RSS 어댑터

**파일:**
- 생성: `src/bullet_in/adapters/rss.py`
- 테스트: `tests/test_rss_adapter.py`, `tests/fixtures/sample_feed.xml`

- [ ] **Step 1: 픽스처 `tests/fixtures/sample_feed.xml` 생성**

```xml
<?xml version="1.0"?>
<rss version="2.0"><channel>
  <item><title>Arteta on win</title><link>https://www.arsenal.com/news/arteta-win</link>
    <pubDate>Tue, 27 May 2026 10:00:00 GMT</pubDate>
    <description>Manager reacts.</description></item>
</channel></rss>
```

- [ ] **Step 2: 실패하는 테스트 작성**

```python
# tests/test_rss_adapter.py
import asyncio
from pathlib import Path
from bullet_in.adapters.rss import RssAdapter

FEED = (Path(__file__).parent / "fixtures" / "sample_feed.xml").as_uri()

def test_rss_adapter_parses_items():
    a = RssAdapter(source_id="arsenal_official", feed_url=FEED)
    items = asyncio.run(a.fetch())
    assert len(items) == 1
    assert items[0].raw_payload["title"] == "Arteta on win"
    assert items[0].url == "https://www.arsenal.com/news/arteta-win"
    assert items[0].source_type == "rss"
```

- [ ] **Step 3: 실행해서 실패 확인**

실행: `uv run pytest tests/test_rss_adapter.py -v`
기대: FAIL — 모듈 없음.

- [ ] **Step 4: 구현**

```python
# src/bullet_in/adapters/rss.py
from __future__ import annotations
from datetime import datetime, timezone
import asyncio, feedparser
from bullet_in.models import RawItem

class RssAdapter:
    source_type = "rss"
    def __init__(self, source_id: str, feed_url: str):
        self.source_id = source_id
        self.feed_url = feed_url
    async def fetch(self) -> list[RawItem]:
        parsed = await asyncio.to_thread(feedparser.parse, self.feed_url)
        now = datetime.now(timezone.utc)
        out: list[RawItem] = []
        for e in parsed.entries:
            out.append(RawItem(
                source_id=self.source_id, source_type="rss", url=e.link,
                fetched_at=now,
                raw_payload={"title": e.title,
                             "published": e.get("published"),
                             "summary": e.get("summary", "")}))
        return out
```

- [ ] **Step 5: 실행해서 통과 확인**

실행: `uv run pytest tests/test_rss_adapter.py -v`
기대: PASS.

- [ ] **Step 6: 실제 피드 URL 확인** — `config/sources.yaml`의 `arsenal_official.feed_url`을 브라우저로 연다. 404면 동작하는 Arsenal 뉴스 RSS URL로 교체한다. 그 후 커밋.

```bash
git add src/bullet_in/adapters/rss.py tests/test_rss_adapter.py tests/fixtures/sample_feed.xml config/sources.yaml
git commit -m "feat: add RSS adapter"
```

### Task 2.3: Guardian API 어댑터

**파일:**
- 생성: `src/bullet_in/adapters/guardian_api.py`
- 테스트: `tests/test_guardian_adapter.py`

- [ ] **Step 1: 실패하는 테스트 작성 (respx로 HTTP 모킹)**

```python
# tests/test_guardian_adapter.py
import asyncio, respx, httpx
from bullet_in.adapters.guardian_api import GuardianAdapter

@respx.mock
def test_guardian_adapter_maps_results():
    respx.get("https://content.guardianapis.com/search").mock(
        return_value=httpx.Response(200, json={"response": {"results": [
            {"webTitle": "Arsenal sign X", "webUrl": "https://g.test/1",
             "webPublicationDate": "2026-05-27T09:00:00Z",
             "fields": {"trailText": "deal done"}}]}}))
    a = GuardianAdapter(source_id="guardian", api_key="k", query="Arsenal")
    items = asyncio.run(a.fetch())
    assert items[0].url == "https://g.test/1"
    assert items[0].raw_payload["title"] == "Arsenal sign X"
    assert items[0].source_type == "api"
```

- [ ] **Step 2: 실행해서 실패 확인**

실행: `uv run pytest tests/test_guardian_adapter.py -v`
기대: FAIL — 모듈 없음.

- [ ] **Step 3: 구현**

```python
# src/bullet_in/adapters/guardian_api.py
from __future__ import annotations
from datetime import datetime, timezone
import httpx
from bullet_in.models import RawItem

class GuardianAdapter:
    source_type = "api"
    BASE = "https://content.guardianapis.com/search"
    def __init__(self, source_id: str, api_key: str, query: str = "Arsenal",
                 section: str = "football"):
        self.source_id = source_id
        self.params = {"q": query, "section": section, "api-key": api_key,
                       "show-fields": "trailText", "order-by": "newest"}
    async def fetch(self) -> list[RawItem]:
        async with httpx.AsyncClient(timeout=20) as client:
            r = await client.get(self.BASE, params=self.params)
            r.raise_for_status()
            results = r.json()["response"]["results"]
        now = datetime.now(timezone.utc)
        return [RawItem(source_id=self.source_id, source_type="api", url=x["webUrl"],
                        fetched_at=now,
                        raw_payload={"title": x["webTitle"],
                                     "published": x.get("webPublicationDate"),
                                     "summary": x.get("fields", {}).get("trailText", "")})
                for x in results]
```

- [ ] **Step 4: 실행해서 통과 확인**

실행: `uv run pytest tests/test_guardian_adapter.py -v`
기대: PASS.

- [ ] **Step 5: 커밋**

```bash
git add src/bullet_in/adapters/guardian_api.py tests/test_guardian_adapter.py
git commit -m "feat: add Guardian API adapter"
```

### Task 2.4: 정적 HTML 어댑터

**파일:**
- 생성: `src/bullet_in/adapters/html.py`
- 테스트: `tests/test_html_adapter.py`, `tests/fixtures/list.html`

- [ ] **Step 1: 픽스처 `tests/fixtures/list.html` 생성**

```html
<html><body>
<a href="/sport/football/articles/abc123">Saka shines</a>
<a href="/sport/football/articles/def456">Rice rested</a>
<a href="/about">About</a>
</body></html>
```

- [ ] **Step 2: 실패하는 테스트 작성**

```python
# tests/test_html_adapter.py
import asyncio, respx, httpx
from pathlib import Path
from bullet_in.adapters.html import HtmlAdapter

HTML = (Path(__file__).parent / "fixtures" / "list.html").read_text()

@respx.mock
def test_html_adapter_extracts_matching_links():
    respx.get("https://bbc.test/arsenal").mock(return_value=httpx.Response(200, text=HTML))
    a = HtmlAdapter(source_id="bbc_sport", list_url="https://bbc.test/arsenal",
                    item_selector="a[href*='/sport/football/articles/']",
                    base_url="https://bbc.test")
    items = asyncio.run(a.fetch())
    assert len(items) == 2
    assert items[0].url == "https://bbc.test/sport/football/articles/abc123"
    assert items[0].raw_payload["title"] == "Saka shines"
    assert items[0].source_type == "html"
```

- [ ] **Step 3: 실행해서 실패 확인**

실행: `uv run pytest tests/test_html_adapter.py -v`
기대: FAIL — 모듈 없음.

- [ ] **Step 4: 구현**

```python
# src/bullet_in/adapters/html.py
from __future__ import annotations
from datetime import datetime, timezone
from urllib.parse import urljoin
import httpx
from bs4 import BeautifulSoup
from bullet_in.models import RawItem

class HtmlAdapter:
    source_type = "html"
    def __init__(self, source_id: str, list_url: str, item_selector: str,
                 base_url: str | None = None):
        self.source_id = source_id
        self.list_url = list_url
        self.item_selector = item_selector
        self.base_url = base_url or list_url
    async def fetch(self) -> list[RawItem]:
        async with httpx.AsyncClient(timeout=20, follow_redirects=True,
                                     headers={"User-Agent": "bullet-in/0.1"}) as c:
            r = await c.get(self.list_url)
            r.raise_for_status()
        soup = BeautifulSoup(r.text, "html.parser")
        now, out, seen = datetime.now(timezone.utc), [], set()
        for a in soup.select(self.item_selector):
            href = a.get("href")
            if not href:
                continue
            url = urljoin(self.base_url, href)
            if url in seen:
                continue
            seen.add(url)
            out.append(RawItem(source_id=self.source_id, source_type="html", url=url,
                               fetched_at=now,
                               raw_payload={"title": a.get_text(strip=True)}))
        return out
```

> `pyproject.toml` 의존성에 `beautifulsoup4>=4.12`를 추가하고 `uv sync --extra dev`를 다시 실행한다.

- [ ] **Step 5: 실행해서 통과 확인**

실행: `uv run pytest tests/test_html_adapter.py -v`
기대: PASS.

- [ ] **Step 6: 커밋**

```bash
git add src/bullet_in/adapters/html.py tests/test_html_adapter.py tests/fixtures/list.html pyproject.toml
git commit -m "feat: add static HTML adapter"
```

### Task 2.5: Playwright 어댑터 (JS 필수 시연)

**파일:**
- 생성: `src/bullet_in/adapters/playwright_news.py`
- 테스트: `tests/test_playwright_adapter.py`, `tests/fixtures/js_list.html`

- [ ] **Step 1: 픽스처 `tests/fixtures/js_list.html` 생성**

```html
<html><body>
<div id="root"></div>
<script>
  setTimeout(() => {
    document.getElementById('root').innerHTML =
      '<a data-testid="article-link" href="https://goal.test/n/1">Late goal</a>';
  }, 50);
</script>
</body></html>
```

- [ ] **Step 2: 실패하는 테스트 작성 (실제 브라우저로 file:// JS 렌더링)**

```python
# tests/test_playwright_adapter.py
import asyncio
from pathlib import Path
from bullet_in.adapters.playwright_news import PlaywrightAdapter

PAGE = (Path(__file__).parent / "fixtures" / "js_list.html").as_uri()

def test_playwright_adapter_reads_js_rendered_links():
    a = PlaywrightAdapter(source_id="goal", list_url=PAGE,
                          item_selector="a[data-testid='article-link']")
    items = asyncio.run(a.fetch())
    assert len(items) == 1
    assert items[0].url == "https://goal.test/n/1"
    assert items[0].raw_payload["title"] == "Late goal"
    assert items[0].source_type == "playwright"
```

- [ ] **Step 3: 실행해서 실패 확인**

실행: `uv run pytest tests/test_playwright_adapter.py -v`
기대: FAIL — 모듈 없음.

- [ ] **Step 4: 구현**

```python
# src/bullet_in/adapters/playwright_news.py
from __future__ import annotations
from datetime import datetime, timezone
from urllib.parse import urljoin
from playwright.async_api import async_playwright
from bullet_in.models import RawItem

class PlaywrightAdapter:
    source_type = "playwright"
    def __init__(self, source_id: str, list_url: str, item_selector: str,
                 base_url: str | None = None, timeout_ms: int = 15000):
        self.source_id = source_id
        self.list_url = list_url
        self.item_selector = item_selector
        self.base_url = base_url or list_url
        self.timeout_ms = timeout_ms
    async def fetch(self) -> list[RawItem]:
        async with async_playwright() as p:
            browser = await p.chromium.launch(headless=True)
            page = await browser.new_page(user_agent="bullet-in/0.1")
            await page.goto(self.list_url, wait_until="domcontentloaded")
            await page.wait_for_selector(self.item_selector, timeout=self.timeout_ms)
            links = await page.eval_on_selector_all(
                self.item_selector,
                "els => els.map(e => ({href: e.href || e.getAttribute('href'),"
                " title: e.textContent.trim()}))")
            await browser.close()
        now, out, seen = datetime.now(timezone.utc), [], set()
        for l in links:
            if not l["href"]:
                continue
            url = urljoin(self.base_url, l["href"])
            if url in seen:
                continue
            seen.add(url)
            out.append(RawItem(source_id=self.source_id, source_type="playwright",
                               url=url, fetched_at=now,
                               raw_payload={"title": l["title"]}))
        return out
```

- [ ] **Step 5: 실행해서 통과 확인**

실행: `uv run pytest tests/test_playwright_adapter.py -v`
기대: PASS (브라우저가 JS 렌더링, 셀렉터 해석).

- [ ] **Step 6: 실제 셀렉터 확인** — 실제 Goal.com URL로 수동 스모크 체크. `item_selector`가 다르면 `config/sources.yaml` 갱신. 안티봇 이슈가 있으면 `docs/troubleshooting/`에 기록.

- [ ] **Step 7: 커밋**

```bash
git add src/bullet_in/adapters/playwright_news.py tests/test_playwright_adapter.py tests/fixtures/js_list.html
git commit -m "feat: add Playwright adapter for JS-rendered sources"
```

### Task 2.6: X (twikit) 어댑터

**파일:**
- 생성: `src/bullet_in/adapters/x_twikit.py`
- 테스트: `tests/test_x_adapter.py`

- [ ] **Step 1: 실패하는 테스트 작성 (네트워크와 분리된 파싱 함수)**

```python
# tests/test_x_adapter.py
from datetime import datetime, timezone
from bullet_in.adapters.x_twikit import tweets_to_items

class FakeTweet:
    def __init__(self, id, text, created_at):
        self.id, self.text, self.created_at = id, text, created_at

def test_tweets_to_items_maps_fields():
    tw = [FakeTweet("1", "Rice rice baby", datetime(2026,5,27,tzinfo=timezone.utc))]
    items = tweets_to_items("x_handofarsnal", "handofarsnal", tw)
    assert items[0].url == "https://x.com/handofarsnal/status/1"
    assert items[0].raw_payload["text"] == "Rice rice baby"
    assert items[0].source_type == "x"
```

- [ ] **Step 2: 실행해서 실패 확인**

실행: `uv run pytest tests/test_x_adapter.py -v`
기대: FAIL — 모듈 없음.

- [ ] **Step 3: 구현 (순수 매퍼 + 얇은 라이브 페처)**

```python
# src/bullet_in/adapters/x_twikit.py
from __future__ import annotations
import os
from datetime import datetime, timezone
from bullet_in.models import RawItem

def tweets_to_items(source_id: str, handle: str, tweets) -> list[RawItem]:
    now = datetime.now(timezone.utc)
    out = []
    for t in tweets:
        out.append(RawItem(
            source_id=source_id, source_type="x",
            url=f"https://x.com/{handle}/status/{t.id}", fetched_at=now,
            raw_payload={"text": t.text,
                         "created_at": str(getattr(t, "created_at", ""))}))
    return out

class XAdapter:
    source_type = "x"
    def __init__(self, source_id: str, handle: str, max_tweets: int = 20):
        self.source_id, self.handle, self.max_tweets = source_id, handle, max_tweets
    async def fetch(self) -> list[RawItem]:
        from twikit import Client
        client = Client("en-US")
        cookies = "x_cookies.json"
        if os.path.exists(cookies):
            client.load_cookies(cookies)
        else:
            await client.login(auth_info_1=os.environ["X_USERNAME"],
                               auth_info_2=os.environ["X_EMAIL"],
                               password=os.environ["X_PASSWORD"])
            client.save_cookies(cookies)
        user = await client.get_user_by_screen_name(self.handle)
        tweets = await user.get_tweets("Tweets", count=self.max_tweets)
        return tweets_to_items(self.source_id, self.handle, tweets)
```

> 버너 (부계정) X 계정을 사용한다. `x_cookies.json`은 `.gitignore`에 명시적으로 추가한다.

- [ ] **Step 4: 실행해서 통과 확인**

실행: `uv run pytest tests/test_x_adapter.py -v`
기대: PASS.

- [ ] **Step 5: 쿠키 파일을 .gitignore에 추가, 커밋**

```bash
echo "x_cookies.json" >> .gitignore
git add src/bullet_in/adapters/x_twikit.py tests/test_x_adapter.py .gitignore
git commit -m "feat: add X (twikit) adapter with isolated mapper"
```

### Task 2.7: 병렬 수집 오케스트레이션

**파일:**
- 생성: `src/bullet_in/ingest.py`
- 테스트: `tests/test_ingest.py`

- [ ] **Step 1: 실패하는 테스트 작성 (소스별 격리 + 동시성)**

```python
# tests/test_ingest.py
import asyncio
from datetime import datetime, timezone
from bullet_in.ingest import gather_all
from bullet_in.models import RawItem

class Ok:
    source_id = "ok"
    async def fetch(self):
        await asyncio.sleep(0.01)
        return [RawItem(source_id="ok", source_type="api", url="https://x.test/1",
                        fetched_at=datetime.now(timezone.utc), raw_payload={})]

class Boom:
    source_id = "boom"
    async def fetch(self):
        raise RuntimeError("down")

def test_gather_isolates_failures():
    items, errors = asyncio.run(gather_all([Ok(), Boom()]))
    assert len(items) == 1 and items[0].source_id == "ok"
    assert errors == {"boom": "down"}
```

- [ ] **Step 2: 실행해서 실패 확인**

실행: `uv run pytest tests/test_ingest.py -v`
기대: FAIL — 모듈 없음.

- [ ] **Step 3: 구현**

```python
# src/bullet_in/ingest.py
from __future__ import annotations
import asyncio
from bullet_in.models import RawItem
from bullet_in.adapters.base import SourceAdapter

async def gather_all(adapters: list[SourceAdapter],
                     concurrency: int = 8) -> tuple[list[RawItem], dict[str, str]]:
    sem = asyncio.Semaphore(concurrency)
    items: list[RawItem] = []
    errors: dict[str, str] = {}
    async def run(a: SourceAdapter):
        async with sem:
            try:
                items.extend(await a.fetch())
            except Exception as e:  # 소스별 격리
                errors[a.source_id] = str(e)
    await asyncio.gather(*(run(a) for a in adapters))
    return items, errors
```

- [ ] **Step 4: 실행해서 통과 확인**

실행: `uv run pytest tests/test_ingest.py -v`
기대: PASS.

- [ ] **Step 5: 커밋**

```bash
git add src/bullet_in/ingest.py tests/test_ingest.py
git commit -m "feat: add asyncio ingest with per-source isolation"
```

### Task 2.8: 설정 기반 어댑터 팩토리

**파일:**
- 생성: `src/bullet_in/adapters/factory.py`
- 테스트: `tests/test_adapter_factory.py`

- [ ] **Step 1: 실패하는 테스트 작성**

```python
# tests/test_adapter_factory.py
from bullet_in.adapters.factory import build_adapters

def test_factory_builds_enabled_adapters(monkeypatch):
    monkeypatch.setenv("GUARDIAN_API_KEY", "k")
    cfg = {"sources": [
        {"source_id": "guardian", "adapter": "guardian_api", "enabled": True,
         "config": {"query": "Arsenal", "section": "football"}},
        {"source_id": "off", "adapter": "rss", "enabled": False, "config": {"feed_url": "x"}},
    ]}
    adapters = build_adapters(cfg)
    assert [a.source_id for a in adapters] == ["guardian"]
```

- [ ] **Step 2: 실행해서 실패 확인**

실행: `uv run pytest tests/test_adapter_factory.py -v`
기대: FAIL — 모듈 없음.

- [ ] **Step 3: 구현**

```python
# src/bullet_in/adapters/factory.py
from __future__ import annotations
import os
from bullet_in.adapters.rss import RssAdapter
from bullet_in.adapters.guardian_api import GuardianAdapter
from bullet_in.adapters.html import HtmlAdapter
from bullet_in.adapters.playwright_news import PlaywrightAdapter
from bullet_in.adapters.x_twikit import XAdapter

def build_adapters(cfg: dict) -> list:
    out = []
    for s in cfg["sources"]:
        if not s.get("enabled", True):
            continue
        c = s.get("config", {})
        kind, sid = s["adapter"], s["source_id"]
        if kind == "rss":
            out.append(RssAdapter(sid, c["feed_url"]))
        elif kind == "guardian_api":
            out.append(GuardianAdapter(sid, os.environ["GUARDIAN_API_KEY"],
                                       c.get("query", "Arsenal"), c.get("section", "football")))
        elif kind == "html":
            out.append(HtmlAdapter(sid, c["list_url"], c["item_selector"], c.get("base_url")))
        elif kind == "playwright":
            out.append(PlaywrightAdapter(sid, c["list_url"], c["item_selector"], c.get("base_url")))
        elif kind == "x_twikit":
            out.append(XAdapter(sid, c["handle"], c.get("max_tweets", 20)))
        else:
            raise ValueError(f"unknown adapter: {kind}")
    return out
```

- [ ] **Step 4: 실행해서 통과 확인**

실행: `uv run pytest tests/test_adapter_factory.py -v`
기대: PASS.

- [ ] **Step 5: 커밋**

```bash
git add src/bullet_in/adapters/factory.py tests/test_adapter_factory.py
git commit -m "feat: add adapter factory driven by sources config"
```

---

## Phase 3 — 저장소

### Task 3.1: MongoDB 원문 랜딩

**파일:**
- 생성: `src/bullet_in/storage/__init__.py`, `src/bullet_in/storage/mongo.py`
- 테스트: `tests/test_mongo_store.py`

- [ ] **Step 1: 실패하는 테스트 작성 (mongomock)**

```python
# tests/test_mongo_store.py
from datetime import datetime, timezone
import mongomock
from bullet_in.storage.mongo import RawStore
from bullet_in.models import RawItem

def _item(url, h):
    return RawItem(source_id="s", source_type="api", url=url,
                  fetched_at=datetime.now(timezone.utc), raw_payload={}, content_hash=h)

def test_insert_is_idempotent_on_content_hash():
    store = RawStore(mongomock.MongoClient().db)
    store.insert_many([_item("https://x.test/a", "h1")])
    store.insert_many([_item("https://x.test/a", "h1")])
    assert store.count() == 1
```

- [ ] **Step 2: 실행해서 실패 확인**

실행: `uv run pytest tests/test_mongo_store.py -v`
기대: FAIL — 모듈 없음.

- [ ] **Step 3: 구현**

```python
# src/bullet_in/storage/__init__.py
```

```python
# src/bullet_in/storage/mongo.py
from __future__ import annotations
from pymongo import ASCENDING
from pymongo.errors import BulkWriteError
from bullet_in.models import RawItem

class RawStore:
    def __init__(self, db):
        self.col = db["raw_items"]
        self.col.create_index([("content_hash", ASCENDING)], unique=True, sparse=True)
    def insert_many(self, items: list[RawItem]) -> int:
        docs = [i.model_dump(mode="json") for i in items if i.content_hash]
        if not docs:
            return 0
        try:
            res = self.col.insert_many(docs, ordered=False)
            return len(res.inserted_ids)
        except BulkWriteError as e:  # 중복은 건너뜀, 치명적 아님
            return e.details.get("nInserted", 0)
    def count(self) -> int:
        return self.col.count_documents({})
```

- [ ] **Step 4: 실행해서 통과 확인**

실행: `uv run pytest tests/test_mongo_store.py -v`
기대: PASS.

- [ ] **Step 5: 커밋**

```bash
git add src/bullet_in/storage/__init__.py src/bullet_in/storage/mongo.py tests/test_mongo_store.py
git commit -m "feat: add MongoDB raw landing store"
```

### Task 3.2: MariaDB 스키마 + 마트 저장소

**파일:**
- 생성: `src/bullet_in/storage/mariadb.py`, `src/bullet_in/storage/schema.sql`
- 테스트: `tests/integration/test_mariadb_store.py`, `tests/integration/__init__.py`, `tests/integration/conftest.py`

- [ ] **Step 1: `src/bullet_in/storage/schema.sql` 생성**

```sql
CREATE TABLE IF NOT EXISTS sources (
  source_id VARCHAR(64) PRIMARY KEY, display_name VARCHAR(128),
  tier FLOAT, medium VARCHAR(32), enabled BOOLEAN DEFAULT TRUE);
CREATE TABLE IF NOT EXISTS articles (
  id BIGINT AUTO_INCREMENT PRIMARY KEY,
  content_hash CHAR(64) NOT NULL UNIQUE,
  url VARCHAR(512) NOT NULL UNIQUE,
  source_id VARCHAR(64), author VARCHAR(128),
  tier FLOAT, confidence_score FLOAT,
  title_original TEXT, title_ko TEXT, summary_ko TEXT, body_excerpt TEXT,
  published_at DATETIME, fetched_at DATETIME,
  revision INT DEFAULT 1,
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
  updated_at DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP);
CREATE TABLE IF NOT EXISTS pipeline_runs (
  run_id VARCHAR(64) PRIMARY KEY, dag_run_id VARCHAR(128),
  started_at DATETIME, finished_at DATETIME, duration_sec FLOAT,
  source_counts JSON, new_count INT, dup_count INT, error_count INT,
  success_rate FLOAT);
```

- [ ] **Step 2: `tests/integration/conftest.py` 생성 (DB 다운 시 skip)**

```python
# tests/integration/conftest.py
import os, pytest
from sqlalchemy import create_engine, text

TEST_URL = os.environ.get("MARIADB_TEST_URL",
    "mysql+pymysql://root:bulletin@localhost:3306/bulletin_test")

@pytest.fixture(scope="session")
def engine():
    base = create_engine("mysql+pymysql://root:bulletin@localhost:3306/")
    try:
        with base.connect() as c:
            c.execute(text("CREATE DATABASE IF NOT EXISTS bulletin_test"))
            c.commit()
    except Exception:
        pytest.skip("MariaDB not available")
    eng = create_engine(TEST_URL)
    from pathlib import Path
    ddl = Path("src/bullet_in/storage/schema.sql").read_text()
    with eng.begin() as c:
        for stmt in [s for s in ddl.split(";") if s.strip()]:
            c.execute(text(stmt))
    yield eng

@pytest.fixture(autouse=True)
def clean(engine):
    with engine.begin() as c:
        c.execute(text("DELETE FROM articles"))
    yield
```

빈 `tests/integration/__init__.py`도 생성한다.

- [ ] **Step 3: 실패하는 테스트 작성**

```python
# tests/integration/test_mariadb_store.py
from datetime import datetime, timezone
from bullet_in.storage.mariadb import MartStore
from bullet_in.models import Article

def _art(h="h1", url="https://x.test/a", title="T"):
    return Article(content_hash=h, url=url, source_id="guardian",
                   title_original=title, published_at=datetime(2026,5,27,tzinfo=timezone.utc))

def test_upsert_dedup_keeps_single_row(engine):
    store = MartStore(engine)
    store.upsert([_art()]); store.upsert([_art()])
    assert store.count() == 1

def test_watermark_returns_seen_map(engine):
    store = MartStore(engine)
    store.upsert([_art()])
    seen = store.seen_map()
    assert seen["https://x.test/a"][0] == "h1"
```

- [ ] **Step 4: 실행해서 실패 확인**

실행: `uv run pytest tests/integration/test_mariadb_store.py -v`
기대: FAIL — 모듈 없음 (DB 다운이면 skip — `docker compose up -d mariadb`로 기동).

- [ ] **Step 5: 구현**

```python
# src/bullet_in/storage/mariadb.py
from __future__ import annotations
from sqlalchemy import text
from sqlalchemy.engine import Engine
from bullet_in.models import Article

class MartStore:
    def __init__(self, engine: Engine):
        self.engine = engine
    def upsert(self, articles: list[Article]) -> int:
        sql = text("""
          INSERT INTO articles
            (content_hash,url,source_id,author,tier,confidence_score,
             title_original,title_ko,summary_ko,body_excerpt,published_at,fetched_at,revision)
          VALUES (:content_hash,:url,:source_id,:author,:tier,:confidence_score,
             :title_original,:title_ko,:summary_ko,:body_excerpt,:published_at,:fetched_at,:revision)
          ON DUPLICATE KEY UPDATE
             revision=VALUES(revision), title_original=VALUES(title_original)""")
        rows = [a.model_dump() for a in articles]
        for r in rows:
            r.setdefault("fetched_at", None)
        with self.engine.begin() as c:
            c.execute(sql, rows)
        return len(rows)
    def count(self) -> int:
        with self.engine.connect() as c:
            return c.execute(text("SELECT COUNT(*) FROM articles")).scalar_one()
    def seen_map(self) -> dict[str, tuple[str, int]]:
        with self.engine.connect() as c:
            rows = c.execute(text("SELECT url,content_hash,revision FROM articles")).all()
        return {u: (h, rev) for u, h, rev in rows}
    def rows_missing_translation(self) -> list[dict]:
        with self.engine.connect() as c:
            rows = c.execute(text(
                "SELECT content_hash,title_original,body_excerpt FROM articles "
                "WHERE title_ko IS NULL")).mappings().all()
        return [dict(r) for r in rows]
    def set_translation(self, content_hash: str, title_ko: str, summary_ko: str):
        with self.engine.begin() as c:
            c.execute(text("UPDATE articles SET title_ko=:t, summary_ko=:s "
                           "WHERE content_hash=:h"),
                      {"t": title_ko, "s": summary_ko, "h": content_hash})
```

- [ ] **Step 6: 실행해서 통과 확인**

실행: `uv run pytest tests/integration/test_mariadb_store.py -v`
기대: PASS (2 passed).

- [ ] **Step 7: 커밋**

```bash
git add src/bullet_in/storage/mariadb.py src/bullet_in/storage/schema.sql tests/integration/
git commit -m "feat: add MariaDB mart store with dedup upsert and watermark"
```

---

## Phase 4 — LLM 인리치먼트

### Task 4.1: 번역 + 요약 (멱등)

**파일:**
- 생성: `src/bullet_in/enrich.py`
- 테스트: `tests/test_enrich.py`

- [ ] **Step 1: 실패하는 테스트 작성 (Anthropic 클라이언트 모킹 + 멱등성)**

```python
# tests/test_enrich.py
from bullet_in.enrich import enrich_rows

class FakeMessages:
    def create(self, **kw):
        class R: pass
        r = R(); r.content = [type("B", (), {"text": '{"title_ko":"제목","summary_ko":"요약"}'})()]
        return r
class FakeClient:
    def __init__(self): self.messages = FakeMessages()

def test_enrich_translates_missing_rows():
    rows = [{"content_hash": "h1", "title_original": "Title", "body_excerpt": "Body"}]
    out = enrich_rows(rows, client=FakeClient(), model="claude-haiku-4-5")
    assert out["h1"] == ("제목", "요약")

def test_enrich_skips_when_no_rows():
    assert enrich_rows([], client=FakeClient(), model="claude-haiku-4-5") == {}
```

- [ ] **Step 2: 실행해서 실패 확인**

실행: `uv run pytest tests/test_enrich.py -v`
기대: FAIL — 모듈 없음.

- [ ] **Step 3: 구현**

```python
# src/bullet_in/enrich.py
from __future__ import annotations
import json

PROMPT = ("Translate the football news title to natural Korean and write a one-line "
          "Korean summary. Return ONLY JSON: "
          '{{"title_ko": "...", "summary_ko": "..."}}\n\nTitle: {title}\nBody: {body}')

def enrich_rows(rows: list[dict], client, model: str) -> dict[str, tuple[str, str]]:
    """rows: content_hash, title_original, body_excerpt 를 가진 dict 목록.
    이미 번역이 없는 행만 전달받는 전제 → 호출 구조상 멱등."""
    result: dict[str, tuple[str, str]] = {}
    for r in rows:
        msg = client.messages.create(
            model=model, max_tokens=300,
            messages=[{"role": "user", "content": PROMPT.format(
                title=r["title_original"], body=r.get("body_excerpt") or "")}])
        data = json.loads(msg.content[0].text)
        result[r["content_hash"]] = (data["title_ko"], data["summary_ko"])
    return result
```

- [ ] **Step 4: 실행해서 통과 확인**

실행: `uv run pytest tests/test_enrich.py -v`
기대: PASS.

- [ ] **Step 5: 커밋**

```bash
git add src/bullet_in/enrich.py tests/test_enrich.py
git commit -m "feat: add idempotent LLM translation/summarization"
```

---

## Phase 5 — 품질 & 메트릭

### Task 5.1: 런타임 이상탐지 + SLO 집계

**파일:**
- 생성: `src/bullet_in/quality.py`
- 테스트: `tests/test_quality.py`

- [ ] **Step 1: 실패하는 테스트 작성**

```python
# tests/test_quality.py
from bullet_in.quality import success_rate, volume_anomaly

def test_success_rate_excludes_errored_sources():
    assert success_rate(total_sources=5, errored=1) == 0.8

def test_volume_anomaly_flags_drop_beyond_threshold():
    # 오늘 값이 추세 평균보다 크게 낮음 -> 이상
    assert volume_anomaly(today=2, history=[20, 22, 18, 21], sigma=2.0) is True

def test_volume_anomaly_ok_within_band():
    assert volume_anomaly(today=20, history=[20, 22, 18, 21], sigma=2.0) is False
```

- [ ] **Step 2: 실행해서 실패 확인**

실행: `uv run pytest tests/test_quality.py -v`
기대: FAIL — 모듈 없음.

- [ ] **Step 3: 구현**

```python
# src/bullet_in/quality.py
from __future__ import annotations
from statistics import mean, pstdev

def success_rate(total_sources: int, errored: int) -> float:
    if total_sources == 0:
        return 0.0
    return round((total_sources - errored) / total_sources, 3)

def volume_anomaly(today: int, history: list[int], sigma: float = 2.0) -> bool:
    if len(history) < 2:
        return False
    mu, sd = mean(history), pstdev(history)
    if sd == 0:
        return today != mu
    return abs(today - mu) > sigma * sd
```

- [ ] **Step 4: 실행해서 통과 확인**

실행: `uv run pytest tests/test_quality.py -v`
기대: PASS (3 passed).

- [ ] **Step 5: 커밋**

```bash
git add src/bullet_in/quality.py tests/test_quality.py
git commit -m "feat: add success-rate and volume-anomaly checks"
```

### Task 5.2: dbt (DuckDB) 프로젝트 + 품질 게이트 테스트

**파일:**
- 생성: `dbt/dbt_project.yml`, `dbt/profiles.yml`, `dbt/models/sources.yml`, `dbt/models/staging/stg_articles.sql`, `dbt/models/marts/daily_source_quality.sql`

- [ ] **Step 1: `dbt/dbt_project.yml` 생성**

```yaml
name: bullet_in
version: "1.0"
profile: bullet_in
model-paths: ["models"]
```

- [ ] **Step 2: `dbt/profiles.yml` 생성 (DuckDB가 MariaDB attach)**

```yaml
bullet_in:
  target: dev
  outputs:
    dev:
      type: duckdb
      path: "bullet_in.duckdb"
      extensions: [mysql_scanner]
      attach:
        - path: "host=localhost user=root password=bulletin port=3306 database=bulletin"
          type: mysql
          alias: maria
```

- [ ] **Step 3: `dbt/models/staging/stg_articles.sql` 생성**

```sql
select content_hash, url, source_id, tier, confidence_score,
       title_original, title_ko, summary_ko, published_at, fetched_at
from maria.articles
```

- [ ] **Step 4: `dbt/models/sources.yml` 생성 (품질 게이트 테스트)**

```yaml
version: 2
models:
  - name: stg_articles
    columns:
      - name: content_hash
        tests: [unique, not_null]
      - name: url
        tests: [unique, not_null]
      - name: title_original
        tests: [not_null]
      - name: tier
        tests:
          - accepted_values:
              values: [0, 1, 1.5, 2, 3, 4]
```

- [ ] **Step 5: `dbt/models/marts/daily_source_quality.sql` 생성**

```sql
select source_id,
       count(*) as n_articles,
       avg(confidence_score) as avg_confidence,
       sum(case when title_ko is null then 1 else 0 end) as untranslated
from {{ ref('stg_articles') }}
group by source_id
```

- [ ] **Step 6: 빌드 + 테스트 확인 (게이트)**

실행: `cd dbt && uv run dbt build --profiles-dir .`
기대: 모델 빌드 성공, `unique`/`not_null`/`accepted_values` 테스트 PASS. 테스트 실패 = 품질 게이트 실패 (의도된 "이상 점검").

- [ ] **Step 7: 커밋**

```bash
git add dbt/
git commit -m "feat: add dbt+duckdb quality gate and source-quality mart"
```

---

## Phase 6 — 서빙

### Task 6.1: 정적 HTML 렌더링

**파일:**
- 생성: `src/bullet_in/serve/__init__.py`, `src/bullet_in/serve/render.py`, `src/bullet_in/serve/templates/index.html.j2`
- 테스트: `tests/test_render.py`

- [ ] **Step 1: `src/bullet_in/serve/templates/index.html.j2` 생성**

```html
<!doctype html><html lang="ko"><head><meta charset="utf-8"><title>Bullet-in</title></head>
<body><h1>Bullet-in — Arsenal 뉴스</h1>
{% for a in articles %}
<article data-tier="{{ a.tier }}">
  <h2>{{ a.title_ko or a.title_original }}</h2>
  <p class="src">{{ a.source_id }} · tier {{ a.tier }}</p>
  <p class="sum">{{ a.summary_ko or "" }}</p>
  <a href="{{ a.url }}">원문</a>
</article>
{% endfor %}</body></html>
```

- [ ] **Step 2: 실패하는 테스트 작성**

```python
# tests/test_render.py
from bullet_in.serve.render import render_page

def test_render_orders_by_confidence_desc_and_shows_korean_title():
    arts = [
        {"title_original": "Low", "title_ko": None, "summary_ko": "", "url": "u1",
         "source_id": "football_london", "tier": 4, "confidence_score": 0.0},
        {"title_original": "High", "title_ko": "높음", "summary_ko": "요약", "url": "u2",
         "source_id": "arsenal_official", "tier": 0, "confidence_score": 1.0},
    ]
    html = render_page(arts)
    assert html.index("높음") < html.index("Low")  # confidence 높은 게 먼저
    assert "원문" in html
```

- [ ] **Step 3: 실행해서 실패 확인**

실행: `uv run pytest tests/test_render.py -v`
기대: FAIL — 모듈 없음.

- [ ] **Step 4: 구현**

```python
# src/bullet_in/serve/__init__.py
```

```python
# src/bullet_in/serve/render.py
from __future__ import annotations
from pathlib import Path
from jinja2 import Environment, FileSystemLoader, select_autoescape

_TPL_DIR = Path(__file__).parent / "templates"

def render_page(articles: list[dict]) -> str:
    ordered = sorted(articles, key=lambda a: a.get("confidence_score") or 0.0, reverse=True)
    env = Environment(loader=FileSystemLoader(_TPL_DIR),
                      autoescape=select_autoescape(["html"]))
    return env.get_template("index.html.j2").render(articles=ordered)

def write_page(articles: list[dict], out_path: str | Path) -> None:
    Path(out_path).parent.mkdir(parents=True, exist_ok=True)
    Path(out_path).write_text(render_page(articles), encoding="utf-8")
```

- [ ] **Step 5: 실행해서 통과 확인**

실행: `uv run pytest tests/test_render.py -v`
기대: PASS.

- [ ] **Step 6: 커밋**

```bash
git add src/bullet_in/serve/ tests/test_render.py
git commit -m "feat: add static HTML rendering ordered by confidence"
```

---

## Phase 7 — 파이프라인 연결 & 오케스트레이션

### Task 7.1: 종단 (end-to-end) 변환 함수

**파일:**
- 생성: `src/bullet_in/pipeline.py`
- 테스트: `tests/test_pipeline.py`

- [ ] **Step 1: 실패하는 테스트 작성 (순수 스테이지를 페이크로 연결)**

```python
# tests/test_pipeline.py
from datetime import datetime, timezone
from bullet_in.pipeline import to_articles
from bullet_in.models import RawItem

def test_to_articles_assigns_hash_tier_confidence_and_dedups():
    raw = [
        RawItem(source_id="arsenal_official", source_type="rss",
                url="https://x.test/a", fetched_at=datetime.now(timezone.utc),
                raw_payload={"title": "Win", "published": "2026-05-27T10:00:00Z"}),
        RawItem(source_id="arsenal_official", source_type="rss",
                url="https://x.test/a?utm_source=x", fetched_at=datetime.now(timezone.utc),
                raw_payload={"title": "Win", "published": "2026-05-27T10:00:00Z"}),
    ]
    sources = {"arsenal_official": {"source_id": "arsenal_official", "tier": 0}}
    arts = to_articles(raw, sources, seen={})
    assert len(arts) == 1                     # utm 변형이 정규화로 중복제거됨
    assert arts[0].tier == 0 and arts[0].confidence_score == 1.0
    assert len(arts[0].content_hash) == 64
```

- [ ] **Step 2: 실행해서 실패 확인**

실행: `uv run pytest tests/test_pipeline.py -v`
기대: FAIL — 모듈 없음.

- [ ] **Step 3: 구현**

```python
# src/bullet_in/pipeline.py
from __future__ import annotations
from datetime import datetime
from dateutil import parser as dtparser
from bullet_in.models import RawItem, Article
from bullet_in.canonical import canonical_url, content_hash
from bullet_in.dedup import classify
from bullet_in.score import confidence

def _published(payload: dict) -> datetime:
    raw = payload.get("published") or payload.get("created_at")
    try:
        return dtparser.parse(raw)
    except (TypeError, ValueError):
        return datetime.now().astimezone()

def to_articles(raw: list[RawItem], sources: dict[str, dict],
                seen: dict[str, tuple[str, int]]) -> list[Article]:
    out: list[Article] = []
    local_seen = dict(seen)
    for item in raw:
        title = item.raw_payload.get("title") or item.raw_payload.get("text") or ""
        url = canonical_url(item.url)
        h = content_hash(title, url)
        decision, rev = classify(url, h, local_seen)
        if decision == "duplicate":
            continue
        local_seen[url] = (h, rev)
        src = sources.get(item.source_id, {})
        out.append(Article(
            content_hash=h, url=url, source_id=item.source_id,
            tier=src.get("tier"), confidence_score=confidence(item.source_id, sources),
            title_original=title, body_excerpt=item.raw_payload.get("summary"),
            published_at=_published(item.raw_payload), revision=rev))
    return out
```

> `pyproject.toml`에 `python-dateutil>=2.9`를 추가하고 `uv sync --extra dev`를 다시 실행한다.

- [ ] **Step 4: 실행해서 통과 확인**

실행: `uv run pytest tests/test_pipeline.py -v`
기대: PASS.

- [ ] **Step 5: 커밋**

```bash
git add src/bullet_in/pipeline.py tests/test_pipeline.py pyproject.toml
git commit -m "feat: add raw->article transform with dedup, tier, confidence"
```

### Task 7.2: CLI 엔트리포인트 (전체 실행)

**파일:**
- 생성: `src/bullet_in/run.py`
- 테스트: 수동 종단 실행

- [ ] **Step 1: 오케스트레이션 엔트리포인트 구현**

```python
# src/bullet_in/run.py
from __future__ import annotations
import argparse, asyncio, json, os, time, uuid, yaml
from pathlib import Path
from pymongo import MongoClient
from sqlalchemy import create_engine, text
import anthropic
from bullet_in.adapters.factory import build_adapters
from bullet_in.ingest import gather_all
from bullet_in.canonical import content_hash, canonical_url
from bullet_in.pipeline import to_articles
from bullet_in.score import load_sources
from bullet_in.storage.mongo import RawStore
from bullet_in.storage.mariadb import MartStore
from bullet_in.enrich import enrich_rows
from bullet_in.serve.render import write_page
from bullet_in.quality import success_rate

async def main(concurrency: int):
    cfg = yaml.safe_load(Path("config/sources.yaml").read_text())
    sources = load_sources("config/sources.yaml")
    adapters = build_adapters(cfg)

    t0 = time.perf_counter()
    started_epoch = time.time()
    raw, errors = await gather_all(adapters, concurrency=concurrency)
    for it in raw:
        it.content_hash = content_hash(
            it.raw_payload.get("title") or it.raw_payload.get("text") or "",
            canonical_url(it.url))

    mongo = MongoClient(os.environ["MONGO_URI"])[os.environ.get("MONGO_DB", "bulletin")]
    RawStore(mongo).insert_many(raw)

    engine = create_engine(os.environ["MARIADB_URL"])
    mart = MartStore(engine)
    arts = to_articles(raw, sources, seen=mart.seen_map())
    mart.upsert(arts)

    client = anthropic.Anthropic(api_key=os.environ["ANTHROPIC_API_KEY"])
    translations = enrich_rows(mart.rows_missing_translation(), client, "claude-haiku-4-5")
    for h, (tk, sk) in translations.items():
        mart.set_translation(h, tk, sk)

    with engine.connect() as c:
        rows = [dict(r) for r in c.execute(text(
            "SELECT title_original,title_ko,summary_ko,url,source_id,tier,confidence_score "
            "FROM articles")).mappings().all()]
    write_page(rows, "site/index.html")

    summary = {"new_or_changed": len(arts), "errors": errors,
               "success_rate": success_rate(len(adapters), len(errors)),
               "elapsed_sec": round(time.perf_counter() - t0, 2)}
    with engine.begin() as c:
        c.execute(text(
            "INSERT INTO pipeline_runs (run_id,dag_run_id,started_at,finished_at,"
            "duration_sec,source_counts,new_count,dup_count,error_count,success_rate) "
            "VALUES (:rid,:drid,FROM_UNIXTIME(:t0),NOW(),:dur,:counts,:new,0,:err,:sr)"),
            {"rid": str(uuid.uuid4()),
             "drid": os.environ.get("AIRFLOW_CTX_DAG_RUN_ID", "manual"),
             "t0": int(started_epoch), "dur": summary["elapsed_sec"],
             "counts": json.dumps({a.source_id: 0 for a in adapters}),
             "new": len(arts), "err": len(errors), "sr": summary["success_rate"]})
    print(summary)

if __name__ == "__main__":
    ap = argparse.ArgumentParser()
    ap.add_argument("--concurrency", type=int, default=8)
    asyncio.run(main(ap.parse_args().concurrency))
```

- [ ] **Step 2: 종단 실행 (스토어 기동, `.env` 채움)**

실행: `docker compose up -d && uv run python -m bullet_in.run --concurrency 8`
기대: 요약 dict 출력, `site/index.html` 생성, confidence 순으로 기사 나열, `pipeline_runs`에 1행 적재.

- [ ] **Step 3: `site/`를 .gitignore에 추가, 커밋**

```bash
echo "site/" >> .gitignore
git add src/bullet_in/run.py .gitignore
git commit -m "feat: add end-to-end pipeline entrypoint with run logging"
```

### Task 7.3: 병렬 speedup 벤치마크 (headline SLO)

**파일:**
- 생성: `src/bullet_in/metrics.py`
- 테스트: `tests/test_metrics.py`

- [ ] **Step 1: 실패하는 테스트 작성**

```python
# tests/test_metrics.py
from bullet_in.metrics import speedup_pct

def test_speedup_pct_computes_reduction():
    assert speedup_pct(sequential_sec=10.0, parallel_sec=3.0) == 70.0
```

- [ ] **Step 2: 실행해서 실패 확인**

실행: `uv run pytest tests/test_metrics.py -v`
기대: FAIL — 모듈 없음.

- [ ] **Step 3: 구현**

```python
# src/bullet_in/metrics.py
from __future__ import annotations
import time
from bullet_in.ingest import gather_all

def speedup_pct(sequential_sec: float, parallel_sec: float) -> float:
    return round((1 - parallel_sec / sequential_sec) * 100, 1)

async def benchmark(adapters) -> dict:
    t = time.perf_counter(); await gather_all(adapters, concurrency=1)
    seq = time.perf_counter() - t
    t = time.perf_counter(); await gather_all(adapters, concurrency=len(adapters) or 1)
    par = time.perf_counter() - t
    return {"sequential_sec": round(seq, 2), "parallel_sec": round(par, 2),
            "speedup_pct": speedup_pct(seq, par)}
```

- [ ] **Step 4: 실행해서 통과 확인**

실행: `uv run pytest tests/test_metrics.py -v`
기대: PASS.

- [ ] **Step 5: 실측값 기록** — 아래를 실행해 결과를 README SLO 표에 붙여넣는다. 그 후 커밋.

```bash
uv run python -c "import asyncio,yaml; from bullet_in.adapters.factory import build_adapters; from bullet_in.metrics import benchmark; print(asyncio.run(benchmark(build_adapters(yaml.safe_load(open('config/sources.yaml'))))))"
git add src/bullet_in/metrics.py tests/test_metrics.py
git commit -m "feat: add parallel-speedup benchmark"
```

### Task 7.4: Airflow 2.x DAG

**파일:**
- 생성: `airflow/dags/bullet_in_daily.py`, `airflow/requirements.txt`
- 테스트: `tests/test_dag_import.py`

- [ ] **Step 1: `airflow/requirements.txt`에 Airflow 2.9 핀**

```
apache-airflow==2.9.3
```

- [ ] **Step 2: DAG `airflow/dags/bullet_in_daily.py` 생성**

```python
from __future__ import annotations
import pendulum
from airflow import DAG
from airflow.operators.python import PythonOperator

def _run():
    import asyncio
    from bullet_in.run import main
    asyncio.run(main(concurrency=8))

with DAG(
    dag_id="bullet_in_daily",
    schedule="@daily",
    start_date=pendulum.datetime(2026, 5, 1, tz="UTC"),
    catchup=False,
    tags=["bullet-in"],
) as dag:
    PythonOperator(task_id="run_pipeline", python_callable=_run)
```

- [ ] **Step 3: DAG 임포트 테스트 작성**

```python
# tests/test_dag_import.py
import pytest
pytest.importorskip("airflow")
from airflow.models import DagBag

def test_dag_imports_without_errors():
    bag = DagBag(dag_folder="airflow/dags", include_examples=False)
    assert bag.import_errors == {}
    assert "bullet_in_daily" in bag.dags
```

- [ ] **Step 4: 실행해서 통과 확인 (Airflow는 별도 venv로)**

실행: `uv run --with apache-airflow==2.9.3 pytest tests/test_dag_import.py -v`
기대: PASS.

- [ ] **Step 5: 커밋**

```bash
git add airflow/ tests/test_dag_import.py
git commit -m "feat: add Airflow 2.9 daily DAG"
```

### Task 7.5: Airflow 2.9 → 3.0 마이그레이션

**파일:**
- 수정: `airflow/requirements.txt`, `airflow/dags/bullet_in_daily.py`
- 생성: `docs/MIGRATION.md`

- [ ] **Step 1: 핀을 3.0으로 상향**

`airflow/requirements.txt`를 `apache-airflow==3.0.0`으로 변경.

- [ ] **Step 2: DAG에 3.0 임포트/API 변경 적용**

`airflow/dags/bullet_in_daily.py`의 오퍼레이터 임포트를 3.x provider 경로로 변경:

```python
from airflow.providers.standard.operators.python import PythonOperator
```

(`schedule=`, `catchup=False`, `start_date`는 3.0에서도 유효하므로 유지. `execution_date` 사용이 없으므로 그 외 변경 없음.)

- [ ] **Step 3: 3.0에서 DAG 임포트 테스트 재실행**

실행: `uv run --with apache-airflow==3.0.0 --with apache-airflow-providers-standard pytest tests/test_dag_import.py -v`
기대: 3.0에서 PASS.

- [ ] **Step 4: `docs/MIGRATION.md` 작성** — 무엇이 왜 바뀌었는지 정확히 기록:

```markdown
# Airflow 2.9 → 3.0 마이그레이션

## 적용한 변경
- `PythonOperator`가 `airflow.providers.standard.operators.python`로 이동
  (3.0에서 코어 오퍼레이터가 `standard` provider로 분리됨).
- requirements에 `apache-airflow-providers-standard` 추가.
- `execution_date` 사용 없음 확인 (3.0에서 제거, `logical_date`로 대체).
- `catchup=False` 명시 (3.0 기본값도 False).

## 검증
- `tests/test_dag_import.py`가 2.9.3과 3.0.0 양쪽에서 통과 (DagBag 임포트, 에러 없음).

## 참고한 breaking-change 체크리스트
- 오퍼레이터 provider 분리, 스케줄러/UI 재작성, Task Execution API
  (태스크에서 커스텀 DB 접근 없음 — 태스크는 파이프라인 엔트리포인트만 호출).
```

- [ ] **Step 5: 커밋**

```bash
git add airflow/ docs/MIGRATION.md
git commit -m "feat: migrate Airflow 2.9 -> 3.0 with documented changes"
```

---

## Phase 8 — 문서 & README

### Task 8.1: 운영 문서

**파일:**
- 생성: `docs/runbook/2026-05-27-daily-operations.md`, `docs/runbook/2026-05-27-incident-recovery.md`, `docs/troubleshooting/README.md`

- [ ] **Step 1: `docs/runbook/2026-05-27-daily-operations.md` 작성**

```markdown
# 런북 — 일상 운영
1. `docker compose up -d` (mongo, mariadb).
2. DAG `bullet_in_daily` 트리거 (또는 `uv run python -m bullet_in.run`).
3. 실행 요약 확인: `success_rate >= 0.99`, `errors == {}`.
4. `site/index.html` 확인.
5. 품질 게이트: `cd dbt && uv run dbt build --profiles-dir .` — 모든 테스트 통과.
```

- [ ] **Step 2: `docs/runbook/2026-05-27-incident-recovery.md` 작성**

```markdown
# 런북 — 장애 복구
- **특정 소스 에러:** 설계상 격리됨. `errors` 맵 확인, troubleshooting/ 참고.
- **재스크래핑 없이 재처리:** MongoDB `raw_items`에서 변환 단계만 재실행.
- **X 세션 만료:** `x_cookies.json` 삭제 후 재실행하여 재로그인 (버너 계정).
- **품질 게이트 실패:** 실패한 dbt 테스트 확인 → 데이터/파서 수정 → 재실행.
```

- [ ] **Step 3: `docs/troubleshooting/README.md` 작성**

```markdown
# 트러블슈팅 로그
인시던트 1건당 파일 1개: `YYYY-MM-DD-<슬러그>.md` (증상 / 원인 / 해결 / 예방).
발생 시 추가할 시드 항목: X 안티봇, Playwright 셀렉터 드리프트, dedup 충돌.
```

- [ ] **Step 4: 커밋**

```bash
git add docs/runbook/ docs/troubleshooting/
git commit -m "docs: add runbook and troubleshooting log scaffold"
```

### Task 8.2: README

**파일:**
- 생성: `README.md`

- [ ] **Step 1: `README.md` 작성** — 스펙 §12 골격을 따른다 (1 한 줄 소개+스크린샷, 2 동기, 3 아키텍처 다이어그램, 4 핵심 기능, 5 SLO 표[실측 채움], 6 기술선택 근거 incl. 왜 CDC 제외, 7 데이터 모델, 8 Airflow 2→3 링크, 9 트러블슈팅 링크, 10 실행법, 11 한계 · 향후, 12 윤리 · 법적 고지). SLO 표는 Task 7.3의 실측값으로 채운다.

- [ ] **Step 2: 커밋**

```bash
git add README.md
git commit -m "docs: add portfolio README"
```

---

## 자체 점검 (Self-Review)

**스펙 커버리지 (스펙 § → 태스크):**
- §5 파이프라인 스테이지 → Task 2.x (수집), 3.x (저장), 4.1 (enrich), 5.2 (dbt 게이트), 6.1 (서빙), 7.1–7.2 (연결). ✓
- §6 소스 전략/어댑터 → Task 2.1–2.8 (5종 어댑터 + 팩토리). ✓
- §7 데이터 모델 (Mongo raw, MariaDB articles/sources/pipeline_runs, dbt 마트) → Task 3.1, 3.2, 5.2. `pipeline_runs` 적재는 7.2 run.py에서 수행. ✓
- §8 메커니즘 (dedup, 증분/워터마크, 변경감지, 스코어링, 멱등 enrich) → Task 1.2, 1.3, 1.4, 3.2 (seen_map), 4.1. ✓
- §9 기술 선택 incl. CDC 제외 근거 → README Task 8.2에 문서화. ✓
- §10 SLO (speedup headline, 중복 0%, 성공률 ≥99%, 완전성, freshness, 이상탐지) → Task 7.3 (speedup), 5.2 (중복=unique 테스트, 완전성=not_null), 5.1 (success_rate, 이상탐지). ✓
- §11 디렉토리 구조 → 전 태스크에 걸쳐 실현. ✓
- §13 v1 스코프 incl. Airflow 2→3 → Task 7.4, 7.5. ✓
- §14 윤리 → README + 시크릿/쿠키 `.gitignore`. ✓

**플레이스홀더 스캔:** "TBD/TODO/적절히 처리" 잔여 없음. DOM 셀렉터 확인 (Task 2.2, 2.5)은 데이터 의존 검증 단계이지 로직 플레이스홀더가 아님.

**타입 일관성:** `RawItem`/`Article` 필드가 models.py · 어댑터 · pipeline.py · storage 전반에서 일치. `gather_all` 시그니처가 ingest 테스트와 metrics/run 사용처에서 일치. `MartStore` 메서드 (`upsert`, `seen_map`, `rows_missing_translation`, `set_translation`, `count`)가 storage 테스트 · run.py · enrich 연결에서 일관. `enrich_rows(rows, client, model)` 시그니처가 테스트와 run.py에서 일치. ✓

**점검 중 수정한 사항:** `run.py`의 죽은 코드 라인 제거, `pipeline_runs` 적재 로직을 명시적으로 추가 (run_id=uuid4, dag_run_id는 `AIRFLOW_CTX_DAG_RUN_ID` 환경변수, 없으면 "manual").
