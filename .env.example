MONGO_URI=mongodb://localhost:27017
MONGO_DB=bulletin
MARIADB_URL=mysql+pymysql://root:bulletin@localhost:3306/bulletin
GEMINI_API_KEY=REPLACE
X_USERNAME=REPLACE
X_EMAIL=REPLACE
X_PASSWORD=REPLACE
# GUARDIAN_API_KEY=REPLACE  # guardian 어댑터 코드는 보존됨 — 소스 재활성 시에만 필요
# 계측 — GA4 측정 ID. 비밀이 아니다 (페이지 소스에 그대로 실린다).
# 비우면 계측 스크립트를 아예 안 넣는다 — 로컬 렌더 · 목업이 운영 수치를 흐리지 않게.
GA_MEASUREMENT_ID=G-WTDJN6R4SN

# 행동 기록 원본 (GA4 BigQuery 내보내기). 없으면 그 갈래를 건너뛴다.
GA4_DATASET=bullet-in-analytics.analytics_551139164
